# TODO

| id | title | spec | pri | status | notes |
|----|-------|------|-----|--------|-------|
| T1 | API retry survives endpoint restarts | specs/t1-api-retry-endpoint-restart.md | 1 | done | done 9840aba — 2026-09-20: two meta sessions died to transient muse resets (conn reset ~30s) — chug retries only ~15s (4 attempts, 1-8s backoff). Connection-level errors (reset/refused/timeout) should retry for minutes (e.g. 8 attempts over ~5 min); 4xx stays fail-fast |
| T2 | Per-request activity timeout on LLM calls | specs/t2-llm-activity-timeout.md | 2 | done | done 9840aba — 2026-09-20: a round-2 child hung 5+ min at 0% CPU on a stale HTTP connection; only the 600s read timeout saved it (and the wedge-kill got there first). Add ~180s no-bytes timeout distinct from total read timeout |
| T3 | Fresh run detects spec/ledger mismatch | specs/t3-stale-ledger-detection.md | 2 | done | done dd1322a — fresh runs archive a non-seed LEDGER.md to .chug/LEDGER-<ts>.md and reseed; --resume/chat keep it; spec authored (was in T8's backfill list) |
| T4 | Bash tool prepends ~/.cargo/bin to PATH | specs/t4-bash-cargo-path.md | 1 | done | done 9840aba — 2026-09-21 transcript mining: nearly every command in every session starts with `export PATH="$HOME/.cargo/bin:$PATH"` — children and metas each had to discover `cargo: command not found` individually (several first attempts died outright). chug's bash tool should prepend ~/.cargo/bin itself — it's our own toolchain |
| T5 | edit_file non-unique match returns match line numbers | specs/t5-edit-disambiguate.md | 2 | done | done 9840aba — 2026-09-21 transcript mining: `edit_file: 'old' found 2 times` triggered full-file `git checkout --` reverts + rewrites (3x in one round) instead of a second anchored attempt. On non-unique match, return the line numbers (+few lines context) of every match so the model can disambiguate |
| T6 | HTTP stub tests must not hang gates | specs/t6-stub-test-timeouts.md | 3 | done | done c4f1188 — accept_conn helper (10s accept deadline + read/write timeouts, set_nonblocking(false) for macOS accept inheritance) at every stub accept; python fakes get signal.alarm(120) self-termination; META-SPEC bounded-gates rule (--test-threads=4 + wall-clock cap); 3 new fail-fast tests; suite 6.2s |
| T7 | Fresh run rotates .chug/transcript.jsonl | specs/t7-transcript-rotation.md | 1 | done | done aed54a5 — fresh runs rotate a non-empty transcript to .chug/transcript-<ts>.jsonl before first append; --resume/chat unchanged; shares run_loop startup path with T3 (dd1322a) |
| T8 | TODO↔spec consistency guard + t1-t6 backfill | specs/t8-todo-spec-guard.md | 2 | done | done 98208e0 — tests/todo_consistency.rs guard (red before backfill, green after; deleting any specs/t*.md fails cargo test); t1/t2/t4/t5 done-records + t6 forward spec backfilled; t3 spec authored in dd1322a |
| T9 | goal_rejected message states check env | specs/t9-check-env-honesty.md | 2 | done | done ceac8f7 — rejection message names the shared run_shell wrapper + out-of-cwd prohibition; literals interpolated from tools::CARGO_BIN_REL (new const used by cargo_bin_dir) and CHECK_TIMEOUT_SECS; drift-breaks-build test + end-to-end drive_loop test |
| T10 | Append-only .chug/events.jsonl | specs/t10-events-jsonl.md | 3 | done | done 34e48de — harvested from the self session's budget-aborted run (complete + green: 318/318, clippy). Event enum stream persisted: run start, iterations w/ cumulative tokens, tool results (≤200c previews, durations), verification, goal verdicts, aborts; rotation mirrors transcript; best-effort never aborts |
| T11 | Startup banner: version/commit/cwd/model | specs/t11-startup-banner.md | 4 | done | done 881d83c — build.rs bakes CHUG_GIT_HASH (git rev-parse, "unknown" fallback, env override verified both directions); banner on stderr at run/chat start; run_start events.jsonl gains version/commit (+ chat sessions now write run_start); README updated. (Origin: 2026-09-21 META-META eval, EVALUATION.md I11 — stale-binary confusion twice) |
| T12 | Budget-abort output names model + fallback hint | specs/t12-budget-abort-hint.md | 4 | done | done af84f11 — Llm::model() getter; Event::Aborted gains model + BudgetExceeded{kind,max}; abort output prints model:/budget:/resume-with-fallback hint; eventlog abort line gains model/budget fields; README abort-output bullet; gates 327+3 green (row flip + ledger wrap landed by operator after the session's second budget abort — code was committed, bookkeeping wasn't) |
| T13 | Budget-low warning before abort | specs/t13-budget-low-warning.md | 2 | done | done 3c1c5b8 (impl 96dbe99) — WARN_REMAINING_ITERS=5 / WARN_REMAINING_SECS=300 + pure budget_low_notice helper; one-shot per-kind user-message injection in drive_loop before the LLM call (steering-note mechanism, transcript+memory); 5 new tests (boundaries, one-shot latches, interpolation, scripted max_iters=8 run). Adversarial validation PASS: 9/9 mutations caught. README bullet |
| T14 | Cumulative tokens in console abort/goal output | specs/t14-abort-token-summary.md | 4 | done | done c46136a (impl 82a38d8) — ConsoleSink caches latest cumulative Event::Usage (None until first response → line omitted); `tokens: <in> in / <out> out (cumulative)` printed after summary on GoalAccepted and with model:/budget: on Aborted; 3 with_writers tests. Adversarial validation PASS: 10/10 mutations caught. README abort-output bullet updated in impl commit |
| T15 | Token-denominated budget (--max-tokens) | specs/t15-token-budget.md | 2 | done | done 12a6d20 (impl fe822d1, harvested from a 40/40-iter glm child that died pre-commit; orchestrator fixed one test-fixture arithmetic slip) — BudgetExceeded::Tokens + top-of-iteration check on cumulative usage_in+usage_out (saturating), one-shot WARN_REMAINING_TOKENS=50_000 budget-low leg (byte-identical message when unset), run+chat CLI knobs, eventlog abort line, 7 new tests + control. Adversarial validation PASS: 8 mutants, core all died (3 weak-test survivors noted in EVALUATION Outcomes). README bullets |
| T16 | mcp_http test pins (SPEC-9 R4 carried gaps) | specs/t16-mcp-http-test-pins.md | 3 | done | done 1ec69f4 (impl 8ec604c, glm child committed + self-mutation-checked 7/7) — 4 tests in mcp_http.rs test module (+185/-0, production untouched): exact Accept-header pins (POST pair + listen GET), tools/list-over-SSE framing leg, deadline constants/constructor-default pins + behavioral first-byte-deadline test, all behind the T6 stub harness; suite 347+3 in ~9s. Tests-only item → validation per LOOP-SPEC §2.4 optional; orchestrator gates + non-vacuousness spot-check sufficed |
| T17 | Budget telemetry in events.jsonl: warning injections + configured budgets | specs/t17-budget-events.md | 3 | done | done 96cc8ef (impl 0abc5de, harvested from a 40/40-iter glm child that died pre-commit — 2nd post-T13 J1) — Event::BudgetLow emitted iff a notice is injected (fire-time remaining counts, ≤3/run), serialized as budget_low line (remaining_tokens null when unset); run_start gains max_iters/max_minutes/max_tokens at run+chat call sites; console/TUI sinks silent; warning text/latches byte-identical; README bullet. Adversarial validation VERDICT: PASS — 6/6 mutants died (2 re-killed by the signing validator; T15 adjacency smoke incl.) |
| T18 | Widen budget-low iteration warning margin (WARN_REMAINING_ITERS 5→8) | specs/t18-warn-margin.md | 2 | done | done 9056c78 — 2026-09-25 cycle-4 J6 FIRED: 3 of last 4 children died at the iteration ceiling with wrap unfinished. One constant + J6 doc line + pins + README bullet; impl glm child green first try (17 iters), spec's fire-time "7" was an off-by-one slip — remaining = max_iters − iteration at a 0-based pre-increment boundary, so max_iters=8/WARN=8 fires at remaining==8 on call 1 (adjudicated in the implementer's favor by validator + orchestrator independently). Adversarial validation VERDICT: PASS — 2/2 mutants died (revert-to-5 + <=→< flip), no pin weakened, interpolation tests correctly untouched |
| T19 | Codify the child events-harvest step in LOOP-SPEC (K2) | specs/t19-events-harvest-step.md | 3 | done | done 0dc6ec5 (impl 1e26acc, glm child green first try ~20 iters) — 2026-09-25 cycle-5 eval L1: cycle-5 orchestrator removed /tmp/chug-loop-t18 WITHOUT harvesting — both child event streams + transcripts + impl LEDGER lost (git worktree remove deletes untracked .chug/ silently). LOOP-SPEC §2 step 5 retitled "Harvest, then merge + close" with harvest-before-removal directive (events-t<N>-<role>-<ts>.jsonl impl+validator alike, both t17 precedents named, LEDGER when non-trivial, transcripts operator's choice) + Phase 3 wrap bullet; folded into step 5 (not renumbered) to preserve t13's §2.5 ref. Adversarial validation VERDICT: PASS (6/6 checks; nit fix 0dc6ec5 named both precedents). Cycle 6 practiced it: 4 T19 child artifacts harvested pre-removal |
| T20 | Startup banner + run_start gain cwd worktree HEAD (branch@commit) | specs/t20-banner-worktree-head.md | 3 | done | done f08ea04 — 2026-09-25 cycle-5 eval L2: wrong-HEAD confusion fired twice (cycle-4 T17 validator 2 killed on wrong-HEAD belief; cycle-5 T18 validator opened "I'm on main"). build_info::resolve_head (best-effort runtime git rev-parse, every failure leg → None, first runtime git call in the binary); banner gains head=<branch>@<short> only when resolvable (git-less fallback byte-identical, pinned); run_start gains head_branch/head_commit (null-when-unresolved, pinned); pure seams (no git in banner()/run_start()); 9 new tests incl. broken-.git/missing-git/detached-HEAD legs + non-vacuousness seam; README banner+events bullets. Adversarial validation VERDICT: PASS — 3/3 mutants died (render revert, resolve→None, serialization swap), zero pins weakened, never-fail proven |
| T21 | LOOP-SPEC impl-child template --max-iters 40→50 | specs/t21-child-iteration-headroom.md | 2 | done | done cac4649 — glm impl green first try (~17/40 iters, self-committed). ANOMALY: child ff-merged its own branch into main after goal_complete rejected its worktree run of the spec's check (the check greps MAIN's LOOP-SPEC.md — unsatisfiable from the worktree; spec-authoring lesson: content-checks must not cd to main); orchestrator reviewed the landed diff (+3/−1, exactly per spec) and proceeded. kimi adversarial validation VERDICT: PASS — (a) one functional change, --max-minutes 35/model/goal/polling byte-identical, META-SPEC validator template untouched; (b) cross-refs intact (step-4 fix-ups share the template, t13's §2.5 ref resolves); (c) evidence verified against all three named streams (40/40 aborts confirmed, T20 budget_low@8 + 6-of-35-minutes confirmed); (d) spec check green in main verbatim. 4 artifacts harvested pre-removal (impl+validate events/LEDGERs) |
| T22 | Bash tool description: macOS `timeout` mirage note | specs/t22-bash-timeout-note.md | 3 | done | done 6068654 (cycle 8; glm impl green first try, goal accepted 23/50 iters) — one sentence appended to the bash description with both load-bearing tokens verbatim (`no \`timeout\` command` + `perl -e 'alarm N; exec @ARGV'`), 120s driver-cap wording intact, zero behavioral surface. Pin `bash_description_warns_macos_has_no_timeout_and_pins_perl_alarm_idiom` asserts the LIVE tool_schemas() output (warning-in-context, idiom, 3→4 sentence count, final-position, cap-preservation) — not a copied literal. kimi adversarial validation VERDICT: PASS — 6/6 mutants died (revert-to-pre-T22, idiom corruption, negated-warning non-vacuousness, extra sentence, cap-wording change, note reordering); worktree restored + re-verified green post-mutations. 3 artifacts harvested (impl+validate events, validate LEDGER). Origin: 2026-09-25 cycle-7 eval M2 — TWO exit-127s in one day across two model families (T20 glm impl at ~39/40 lost 2 of its last 3 iters to the mirage, contributing to its 40/40 wrap death; cycle-7 kimi orchestrator hit it live during eval). META-SPEC:126 names the perl-alarm idiom but impl children never read META-SPEC — the tool description is the only universal surface |
| T23 | `delegate` tool: launch + status for bounded child chug runs | specs/t23-delegate-tool.md | 2 | done | done 1012dca (+nit 4169c5d, ff-merged to 4169c5d) — glm impl goal ACCEPTED at 50/50 iters (T21's widened budget + directive warning: committed at ~48, wrapped at 50 — first full wrap in the ceiling zone; mid-wrap it survived a flaky-test hunt at iters 43-47). 887-line diff: one tool two actions (launch=detached spawn, CHUG_DELEGATE_BIN seam→current_exe, <cwd>/.chug/delegate.log, returns pid/log/events at spawn; status=bounded ≤64KiB events tail via pure summarize_events + kill(pid,0) + log tail), process_group(0)+SIGHUP-IGN nohup parity, resolve_safe exemption confined+pinned, 18 new tests, README bullets. kimi validator VERDICT: PASS (10 findings, 6/6 legs: gates re-run green post-restore, 2/2 mutants died — flag-revert killed 3 pins, bound-removal killed the tail pin, non-blocking verified by reading, kill_pid_group extraction byte-identical, zero pre-T23 pins touched); nit (README comma-list) fixed by orchestrator 4169c5d. 4 artifacts harvested pre-removal |
 | T24 | LOOP-SPEC adopts the `delegate` tool for child launches + polling | specs/t24-loop-adopts-delegate.md | 2 | done | done e913cf9 (merge of impl 3334743, cycle 10) — 2026-09-25 cycle-9 eval N5: delegate (T23, 1012dca) had ZERO calls in cycle-7/8 loop streams (jq-verified); §2 taught hand-rolled nohup + ps/tail/jq polling. §2 step 2 now launches via delegate (action launch, max_iters 50/max_minutes 35 explicit — delegate defaults 40/35, T21 headroom survives), polls via delegate status (~60–110s cadence, replaces ps+tail+jq triple; liveness-unknown → ps fallback), hand-rolled nohup kept as tool-failure fallback (1 sentence, only remaining nohup mention); step 4 validator launches via delegate (kimi-k3, 40/30 per §6 budgets, §6 goal verbatim — LOOP-SPEC override of §6 mechanics; META-SPEC.md untouched); hard rule gains delegate-as-launch-surface clause; steps 1–6 unrenumbered (t13 §2.5 ref resolves). kimi adversarial validation (FIRST live delegate launch — the change dogfooded itself) VERDICT: PASS at 13/40 iters ~4 min: all 6 reqs verified, goal text byte-identical (cmp-verified), budgets match T21/T23 ground truth, gates re-run independently (build+clippy+392/3), mutation-tested (goal-corruption + 50→40 mutants caught; 2 non-blocking observations: whole-file grep non-localizing, pre-image passes check leg by design). 2 validate artifacts harvested pre-removal (impl's 2 harvested in cycle 9) | 
 | T25 | Failure-aware event previews: error tool results keep a tail window | specs/t25-failure-aware-previews.md | 2 | done | done f1f7946 (cycle 9; glm impl goal accepted 34/50 iters, self-committed) — 2026-09-25 cycle-9 eval N1: T23-impl suite FAILED once at 20:17:49 (379/380) — flat 200c head preview (driver.rs:667 take(500) → eventlog.rs:179 take(200)) lost the failing test's name; transcript unharvested; 5× suite re-runs green, no repro. Failure bytes cluster at output END — error leg gains tail-anchored ≤2000c window; ok leg byte-identical both seams. Next flake names itself from the events stream alone. VALIDATION VERDICT: PASS (kimi, 8 mutants: 7 died incl. all 3 spec-required — driver take(500)/sink take(200)/head-instead-of-tail + off-by-one/const/ok-leg mutants; 1 non-blocking survivor: driver const 2000→1999 const-relative assertions, noted). Ok legs byte-identical both seams; re-anchored 200c pin retains teeth on ok:true; README events bullet updated; 3 artifacts harvested pre-removal | 
 | T26 | `read_file` gains `offset`/`limit` pagination | specs/t26-read-file-pagination.md | 3 | done | done 179e1aa (merge of impl 66a7ab8, cycle 10) — 2026-09-25 cycle-9 eval §3: FOUR files exceeded the 2000-line read cap (driver.rs 2894, mcp_http.rs 2343, tui.rs 2163, tools.rs 2048) with no paging primitive → children sed-chunked (T20 impl burned iters 1–5). Schema gains optional integer offset (1-based, default 1) + limit (default READ_MAX_LINES, may exceed); default path byte-identical pinned (legacy note verbatim); window legs note the true window [showing lines A-B of N]; offset<1 → tool error naming 1-based; offset>EOF non-error naming length (paging loops stop cleanly); saturating arithmetic; negative/non-integer rejected; README Tools one-liner updated; spec check fixed worktree-relative pre-dispatch (1d6780d, T21 lesson). glm impl 27/50 ~5 min first-try green; kimi VERDICT: PASS — 8/8 mutants died (note drift/0-based/+limit-vs-−1/>=EOF/always-note/schema-drop/required+/full revert), 3 non-blocking observations (past-EOF contains-assertion strength, negative-offset wording, whole-file trailing-newline nuance). 3 artifacts harvested pre-removal | 
| T27 | loopd cycle budget --max-iters 80→120 | specs/t27-loopd-iteration-budget.md | 2 | done | done 759c45e (merge of impl a436baf, cycle 11) — 2026-09-25 cycle-11 eval O1: orchestrator's own iteration ceiling is now the binding budget — cycle 9 ended 79/80 (budget_low@8 21:14:00 forced directive wrap, T24 validation deferred a full cycle); cycle 7 was 76/80; freshness-skip cycles 41/68. Measured: Phase 1 ≈45 iters (eval commit at tool idx 47/91), one item end-to-end ≈30–35; minutes never binding (24–35 of 240). T21's failure class one level up. One number at loopd.sh:71 +1 comment line with the arithmetic; commit message records the no-signal-running-supervisor rationale (bash incremental-read hazard, bounded blast radius); activation = operator's next loopd restart (human-decision carry EVALUATION §6.8). glm impl 12/50 iters goal-accepted (survived a 50-min system hibernate mid-run — machine slept 21:50–22:40Z at 1% battery; macOS Instant excludes sleep so awake-time budgets held; NEW incident class, survived gracefully). kimi VERDICT: PASS 16/40 — gates re-run 403+3, goal text + mode cmp-verified, 6 mutants: 5 caught (revert-80/dup-line/121/drop-minutes/wrong-model), M2 same-line-dup escapes grep -c but clap rejects dup args at runtime (informational), M6 comment-removal in-spec-scope (verified by review). 3 artifacts harvested pre-removal |
| T28 | delegate status reaps zombie children (waitpid WNOHANG) | specs/t28-delegate-zombie-reap.md | 3 | done | done fcaa7c2 (cycle 12; impl a0a7c97 harvested uncommitted from glm child pid 44199, 50/50 budget-abort pre-commit — cycle-11 recovery executed per its own instructions; kimi VERDICT: PASS goal-accepted 34/40: gates 4x green, 4 mutants 3 caught incl. reap-disable non-vacuousness proof, M3 one-poll-lie informational survivor; live proof during validation — exited validator read STAT Z while pre-T28 status said alive:true; 4 artifacts harvested, cycle-11 partials superseded+removed) — 2026-09-25 cycle-11 eval O2: all three cycle-10 children polled alive:true AFTER state:done+goal_seen:true (21:22:47/21:31:59/21:37:39) — kill(pid,0) succeeds on unreaped zombies and the orchestrating chug never reaps. status attempts waitpid(pid, WNOHANG) first (own exited child → reaped → alive:false), ECHILD → existing kill(pid,0) semantics unchanged; launch untouched; output shape unchanged. reap_and_alive seam + pid-0/over-i32 guard; 4 tests cover spec items 1–5 |
| T29 | delegate status gains optional wait_secs long-poll | specs/t29-delegate-wait-secs.md | 3 | done | done 47dfcaf (cycle 13 — recovered mid-arc from the prior cycle's preserved worktree: impl 549d250 (glm) + validator #1 VERDICT: FAIL (M7 liveness-flip mutant SURVIVED — req 2(b) untested; deadline leg rendered stale ENTRY snapshot with no final read) → glm fix-up e36b30c (real-process liveness-flip test, sleep-30 SIGKILLed mid-wait, M7-mutant dies at 30.17s; deadline leg gains one final read + pin, revert-mutant dies 2.08s) → kimi re-validation VERDICT: PASS 36/40 (M1/M2/M3 mutants all caught incl. both fix sites, gates re-run 415+3 twice, 2 informational edge-leg nits); 8 artifacts harvested (4 events + 4 LEDGERs incl. FAIL+PASS verdict ledgers)) — 2026-09-25 cycle-11 eval O7 (FEATURE): cycle 10 spent 9/68 iterations (~13%) on instant status polls, each a full-context round trip (~325k input tokens late-cycle) — idle waiting is the loop's largest remaining mechanical token sink. status gains optional integer wait_secs (0/absent = instant, byte-identical pinned; >0 = block until events-state change / liveness flip / deadline, hard cap 600s, 2.5s cadence, +waited:<n>s line; rejected on launch naming status-only; REJECT not clamp for negative/non-integer/>600, pinned; missing-file legs wait bounded w/ creation as wake trigger, mid-wait read failure degrades instant); LOOP-SPEC §2 step-2 polling sentence gains the wait clause (ship+adopt in one item, T23→T24 pattern); README clause; 8 test legs incl. anti-fixed-sleep non-vacuousness. Schema description + delegate tool surface updated live |
| T30 | META-META-SPEC spec-bar: check: lines must be worktree-relative | specs/t30-metameta-worktree-relative-check.md | 3 | done | done 00ce617 (cycle 13; impl 4b55d61 glm 19/50 ~5 min first-try) — 2026-09-25 cycle-11 eval (cycle-10 wrap recommendation): the worktree-relative-check lesson is per-spec-author, not per-spec — T26's spec shipped a cd-to-main check (fixed pre-dispatch 1d6780d) one cycle after T24's correct one; T21's self-merge anomaly (cac4649) was the first bite. Spec-quality-bar sentence in "Extend TODO.md" extended in place (+4/−1, one hunk): the check: line MUST be worktree-relative (runs in the impl child's worktree cwd, never cd to main; content checks grep worktree, cargo test as-is), both bites cited; every other line byte-identical incl. 9b7e904's §6 README-audit. kimi adversarial validation VERDICT: PASS 19/40 — citations verified against git record (cac4649/e73b66d/1d6780d/3334743), Phase-2-sanction-vs-hard-rules coherence adjudicated (hard rules bind the evaluator role; META-META-SPEC.md not in its enumerated human-spec list), check-mutations M1/M2/M3 (drop worktree-relative / drop T21 / full revert) all killed, spec check verbatim green, gates re-run 415+3, worktree restored clean. 4 artifacts harvested |
| T31 | Deflake the parallel-load test family (mechanism, not timeouts) | specs/t31-deflake-parallel-suite.md | 1 | done | done e765de2 (impl f8012c2, glm child goal-accepted 44/50 ~11 min, budget_low@8 fired at 42 — T18 margin did its job; tests-only, all hunks in mod tests) — dead_port() probe-verified refused-port acquisition (bounded 32-attempt re-bind + pre-connect re-verify + live/dead pin dead_port_probe_distinguishes_live_from_dead) kills the bind-:0-drop TOCTOU that rejected T28's goal_complete; RUN_SHELL_TIMING_LOCK static-Mutex serializes the 3 wall-clock run_shell tests (poison-tolerant guard, std-only) so suite-manufactured scheduler stretch can no longer co-occur with a clocked window; ZERO timeout/grace/slack constants touched, all elapsed bounds byte-identical; 3 mutants killed by the child + probe-mutant independently re-killed by the orchestrator (pin dies 0.00s); 8 consecutive full-suite runs green (5 child + 3 orchestrator). Validation: tests-only per §2.4 optional (T16 precedent) — orchestrator gates + spot-check sufficed. 2 artifacts harvested pre-removal. Origin: 2026-09-25 cycle-14 eval P1 — 4 sightings / 3 named tests (dead_server_retries port TOCTOU mcp_http.rs:1484; run_shell READER_GRACE wall-clock tools.rs:1744,1782) |
| T32 | LOOP-SPEC validation-child template --max-iters 40→50 | specs/t32-validator-iteration-headroom.md | 2 | done | done a3d60ad (impl a173ff2, glm 11/50 ~3 min first-try) — one hunk, LOOP-SPEC §2 step 4 only: `max_iters: 50`, `max_minutes: 30` + parenthetical reworded "§6's budgets with T21-class widened iterations" (minutes rationale verbatim; META-SPEC.md untouched per T21/T24). kimi VERDICT: PASS 12/40 — byte-diff vs parent verified, gates 416+3 re-run independently, 4/4 budget mutants killed by the spec check, sole survivor parenthetical-only revert (by design, review-covered). SELF-REFERENCE: T32's own validator ran at the PRE-T32 40/30 budget — the 50 lands only after this merge. 2 artifacts harvested pre-removal (impl + validate streams; both LEDGERs seed-trivial, skipped per doctrine). Origin: 2026-09-25 cycle-14 eval P2 — 4 of last 6 validators ≥34/40, t29-validate1 FAIL at 39/40 |
| T33 | META-META-SPEC priority line: align with LOOP-SPEC amended doctrine | specs/t33-metameta-priority-line.md | 3 | done | done 0ee166b (impl 4b409c0, glm 16/50 ~4 min first-try) — one hunk: "bugs > robustness > features > DX friction > performance — features are first-class (LOOP-SPEC §2): at equal pri, a credible feature row is worked before a DX-friction row"; T30 check: sentence + T21/T26 bites byte-identical; LOOP-SPEC/README untouched. kimi VERDICT: PASS 12/50 — FIRST validator at the T32-widened budget; features↔robustness + DX↔performance swap mutants killed by the check; clause-drop survivor inherent to spec-dictated check (R1 met by inspection); check proven non-vacuous (fails on parent); adjudication citation verified real at EVALUATION.md:129; gates 419 re-run independently. 3 artifacts harvested pre-removal (impl + validate streams, non-trivial validate LEDGER). Origin: 2026-09-25 cycle-14 eval P3 — cycle-13 wrap handoff, adjudicated: LOOP-SPEC wins |
| T34 | LOOP-SPEC: Outcomes entries written per-item at landing, not deferred to wrap | specs/t34-per-item-outcomes.md | 3 | done | done dcbff00 (impl a965053, glm 14/50 ~4 min first-try) — two hunks, LOOP-SPEC.md only: §2 step 5 gains the per-item Outcomes directive (entry in the row-flip commit or an immediately following eval: commit; cycle-12 bite named inline with the 697a6b6 lesson quoted); Phase 3 Outcomes bullet re-scoped from wrap-authorship to wrap-assembly + gap-check. kimi VERDICT: PASS 11/50 — 3/3 check mutants killed (drop per-item / drop cycle-12 / restore old bullet), check non-vacuous on parent, grounding fact-checked (EVALUATION.md:608 "RECONSTRUCTED at cycle-13 wrap" + commit 697a6b6 real), gates 416+3 re-run. 3 artifacts harvested pre-removal (impl + validate streams, non-trivial validate LEDGER). NOTE: T31-T33 landed this cycle BEFORE the doctrine — their per-item entries are written retroactively in the cycle-14 wrap's immediately-following eval: commit, which is exactly the pattern the doctrine prescribes going forward. Origin: 2026-09-25 cycle-14 eval P4 |
| T35 | README quickstart: cargo install --path . so commands work as written | specs/t35-readme-quickstart-path.md | 3 | done | done 09730de (impl 8ebbe59, glm 10/50 ~2 min first-try) — install step immediately after `cargo build` with the PATH comment (+1/−0, README.md only, rest of Quickstart + Development byte-identical); docs-only → §2 step 4 validation optional (T16/T31 precedent): orchestrator re-ran gates independently (416+3 green worktree AND main, clippy -D warnings clean, spec check verbatim both trees) + diff review. 2 artifacts harvested pre-removal (impl events + LEDGER). Origin: 2026-09-25 cycle-14 eval §6(e) (first README-usability-audit cycle): quickstart ran `cargo build` then bare `chug` — failed as written for a cold reader (verified live: `which chug` empty, ~/.cargo/bin/chug absent, operator ran ./target/debug/chug) |
| T36 | loopd cycle budget --max-iters 120→160 | specs/t36-loopd-iteration-budget-160.md | 2 | done | done 0586e1e (impl d5141c9, glm 15/50 ~2.5 min first-try) — 2026-09-26 cycle-16 eval Q1: BOTH 120-cap fresh-eval cycles scraped the ceiling — cycle 13 at 116/120, cycle 14 at 119/120 (budget_low@8 both; T27 filed on the identical 2-data-point pattern at the 80 cap). Cost model updated for wait_secs: Phase 1 ≈45–55 (§6 audit added), item ≈28–35, wrap ≈10 → eval+3 items ≈150–165 > 120. Deferral absorbs overflow (T35 worked) but budget_low wraps squeeze Phase 3 = the next cycle's input. Minutes never binding (56–117 of 240). Fourth of the class (T21 children, T32 validators, T27 orchestrator) |
| T37 | web_fetch tool: read-only HTTP(S) GET, bounded | specs/t37-web-fetch-tool.md | 3 | done | done 1e82ffb — 2026-09-26 cycle-17: recovered per T29 precedent from the cycle-16 preserved worktree (impl 1432a2e, harvest-completed by the cycle-16 orchestrator after the glm child died 50/50 mid-gates). Gates re-run in the worktree (430+3, clippy clean) then kimi adversarial validation via delegate (REQUIRED — src/tools.rs) VERDICT: PASS 48/50 (budget_low@8 fired at 42, wrapped inside T18 margin) — all 7 reqs verified, 10/10 mutants killed (cap-20k→50k, clamp removal, redirect >=→>, scheme-gate drop, binary-accept, strip-off, truncation-removal, non-2xx-arm removal, marker corrupt, whitespace-collapse break — each died to exactly the expected test), gates green pre+post-mutations, worktree restored; 3 non-blocking observations (req-7 out-of-scope not enumerated in the impl commit msg; connect-timeout const not value-pinned; skip_raw_block prefix-matches a closing </scriptx>). Merged 1e82ffb; gates re-run in main (430+3 + clippy clean); 2 artifacts harvested pre-removal (validate events + verdict LEDGER; impl events harvested cycle-16). Origin: 2026-09-26 cycle-16 eval §4 FEATURE — LOOP-SPEC §2 names the gap (a missing delegate/web_fetch tool); delegate landed T23, web_fetch was the remaining half. Demand honest: 6 consecutive evals grep 0 organic web attempts (repo-local work) — filed on capability-gap doctrine (META-META §4, features first-class) + harness-class standard. Strictly less powerful than bash curl — value is the bounded/audited/token-safe surface (size caps, HTML strip, events previews). Shipped: GET only, http/https, redirect cap 5, connect 10s/total 30s, max_chars default 20k/clamp 100k, HTML script/style-strip + tag-strip + whitespace collapse (no new dep), text/JSON verbatim, binary refusal naming type, non-2xx tool error with ≤500c preview, [truncated at N chars] marker, sandbox candor, T6+T31 stub discipline, 9 test legs |
| T38 | Truncated-response advisory: stop_reason=max_tokens names the chunking remedy | specs/t38-max-tokens-truncation-advisory.md | 2 | done | done a686522 — 2026-09-26 cycle-18 eval R1: MAX_TOKENS=8192 (api.rs:10) truncates any single response over 8k output tokens; the T37 glm impl died 50/50 to the truncated-write saga (987-line write_file in one call) with no signal why; the driver keys off tool_use blocks and never acts on stop_reason (api.rs:417). Inject an advisory + events line at T13's seam (driver.rs:509-538) |
| T39 | delegate launch gains optional max_tokens passthrough | specs/t39-delegate-max-tokens.md | 3 | done | done 52ec8ab — 2026-09-26 cycle-19: glm impl 30/50 first-try; kimi VERDICT PASS 38/50 — 5/5 reqs hand-verified, gates re-run 444+6+3+clippy, M1/M2/M4/M5 mutants KILLED (drop-append, accept-0, drop-echo, drop-schema-minimum), M3 as_u64-for-as_i64 survivor adjudicated spec-neutral (both error legs name the constraint; 0/negatives never spawn). delegate_child_argv pure seam (absent = byte-identical pre-T39 argv, pinned whole-list + end-to-end via CHUG_DELEGATE_BIN stub), delegate_max_tokens parse (<1 → error naming constraint, 0-would-mean-unlimited rationale in doc comment), schema optional integer minimum 1, return-text echo only when configured, README launch-sentence clause. 3 artifacts harvested (impl LEDGER overwritten pre-archive by the validator's fresh-run reseed — impl events + validator verdict LEDGER kept) |
| T40 | LOOP-SPEC step-5 doctrine: TODO notes cells must contain no pipe char + run todo_consistency after edits | specs/t40-todo-pipe-doctrine.md | 3 | done | done 75c21c8 — 2026-09-26 cycle-20: recovered from cycle-19's preserved worktree (impl 311d305, glm 13/50 goal-accepted, orchestrator budget-low pre-review) per T29/T37 precedent. +7 lines LOOP-SPEC step 5, one sentence woven into the row-flip directive. kimi VERDICT: PASS 15/50 — non-vacuousness proven (3 check tokens absent on parent), gates re-run 444+6+3+clippy, M1/M2/M3 token mutants killed, M4 prose-weakening survivor by-design per T44 precedent, tree sha256-restored, citations grounded (75da192 + cycle-18 eval d264c4f). 4 artifacts harvested pre-removal (impl events copied BEFORE the validator launch — T39 reseed lesson). Origin: cycle-18 eval R2 — cycle-16's goal-gate death (pipe in T37 notes cell, goal_complete rejected ~118/120, abort 120/120, fix 75da192) |
| T41 | Sandbox-candor pass: filesystem tool descriptions name the refusal + bash escape; README Tools intro names the web_fetch exception | specs/t41-sandbox-candor-descriptions.md | 4 | todo | 2026-09-26 cycle-18 eval R4: path-escapes-cwd fired twice in cycle 16 (read_file on the /tmp child worktree during review, write_file /tmp scratch) — the descriptions undersell the refusal (tools.rs:65/78/90/128); README says delegate is the one documented exception but web_fetch also reaches outside by design (T37) |
| T42 | Pin webfetch connect/total timeout const values | specs/t42-webfetch-timeout-pins.md | 4 | todo | 2026-09-26 cycle-18 eval: T37 kimi validator non-blocking observation — WEB_FETCH_CONNECT_TIMEOUT (webfetch.rs:54, 10s) and WEB_FETCH_TOTAL_TIMEOUT (:60, 30s) are not value-pinned; a const-only edit would silently drift from the description. Tests-only, validation optional per T16/T31 precedent |
| T43 | EVALUATION.md Outcomes pruning rule (last 6 cycles full) + first compaction of cycles 5-13 | specs/t43-outcomes-pruning.md | 4 | todo | 2026-09-26 cycle-18 eval R3: Outcomes is 50,957 of 65,802 bytes (371 of 625 lines, 77 percent), grows monotonically, and is rewritten wholesale at every fresh eval (~13k tokens of carry-forward); the narratives are redundant with the git row-flip commits |
| T44 | Pipeline overlap: item N+1 impl runs while item N validates | specs/t44-pipeline-overlap.md | 1 | done | done 41f61a1 — 2026-09-26 operator speed initiative ("feels awfully slow"): serial impl→validate→merge pipeline measured — cycle 14 fresh-eval+4 items 56min. Disjoint-file impl N+1 may overlap validator N (max 2 children: 1 validator + 1 impl); merges strictly serial; doctrine items never overlap |
| T45 | Bundle trivial same-area rows into one round | specs/t45-bundle-trivial-rows.md | 2 | done | done 3efb98d — 2026-09-26 cycle-19: glm impl 28/50 first-try; kimi VERDICT PASS 20/50 — 4/4 reqs verified, gates independently re-run (436+6+3 + clippy), M3 TODO-spec-cell mutant KILLED by todo_consistency (T8 guard live), M1/M2 prose mutants by-design survivors per T44 precedent, tree hash-verified restored; 1 non-blocking finding: a bundle's delegate spec: field takes the first row's spec, child reads the rest from its worktree. §2 conjunctive eligibility (a)-(d), one commit per row in queue order, ONE validation round per bundle, shared todo: flip commit, T44 cap interplay (bundle = ONE impl child; doctrine bundle never overlaps). 4 artifacts harvested |
| T46 | Eval digest: pre-computed Phase-1 corpus summary | specs/t46-eval-digest.md | 2 | done | done 6a3fa98 — 2026-09-26 cycle-19 (freshness-skip): glm impl 49/50 goal-accepted (budget_low@8 wrapped inside T18 margin); kimi VERDICT PASS 31/50 — 5/5 reqs hand-verified vs raw jq, gates re-run twice, 6/6 kill-mutants died, 7/7 survivors (unpinned output fields) adjudicated non-blocking per T44 precedent, golden-section pin test recommended as watch item. scripts/eval-digest.sh (293 lines, jq/awk, LC_ALL=C, pinned clock, BSD/GNU fallbacks, ~0.2s) + tests/eval_digest.rs (6) + loopd refresh line + META-META-SPEC corpus item 1 digest-first + README integration; nit 6a3fa98 backticks. 4 artifacts harvested |
| T47 | Shared CARGO_TARGET_DIR for worktree builds | specs/t47-shared-target-dir.md | 3 | todo | 2026-09-26 operator speed initiative: every worktree builds from scratch (43s-6min/round, 2-4 builds per validator round). target-shared/ (NOT repo target/), gitignored, mkdir by loopd, reclaim = operator rm -rf |
