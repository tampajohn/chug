# TODO

| id | title | spec | pri | status | notes |
|----|-------|------|-----|--------|-------|
| T1 | API retry survives endpoint restarts | specs/t1-api-retry-endpoint-restart.md | 1 | done | done 9840aba — 2026-09-20: two meta sessions died to transient muse resets (conn reset ~30s) — chug retries only ~15s (4 attempts, 1-8s backoff). Connection-level errors (reset/refused/timeout) should retry for minutes (e.g. 8 attempts over ~5 min); 4xx stays fail-fast |
| T2 | Per-request activity timeout on LLM calls | specs/t2-llm-activity-timeout.md | 2 | done | done 9840aba — 2026-09-20: a round-2 child hung 5+ min at 0% CPU on a stale HTTP connection; only the 600s read timeout saved it (and the wedge-kill got there first). Add ~180s no-bytes timeout distinct from total read timeout |
| T3 | Fresh run detects spec/ledger mismatch | specs/t3-stale-ledger-detection.md | 2 | done | done dd1322a — fresh runs archive a non-seed LEDGER.md to .chug/LEDGER-<ts>.md and reseed; --resume/chat keep it; spec authored (was in T8's backfill list) |
| T4 | Bash tool prepends ~/.cargo/bin to PATH | specs/t4-bash-cargo-path.md | 1 | done | done 9840aba — 2026-09-21 transcript mining: nearly every command in every session starts with `export PATH="$HOME/.cargo/bin:$PATH"` — children and metas each had to discover `cargo: command not found` individually (several first attempts died outright). chug's bash tool should prepend ~/.cargo/bin itself — it's our own toolchain |
| T5 | edit_file non-unique match returns match line numbers | specs/t5-edit-disambiguate.md | 2 | done | done 9840aba — 2026-09-21 transcript mining: `edit_file: 'old' found 2 times` triggered full-file `git checkout --` reverts + rewrites (3x in one round) instead of a second anchored attempt. On non-unique match, return the line numbers (+few lines context) of every match so the model can disambiguate |
| T6 | HTTP stub tests must not hang gates | specs/t6-stub-test-timeouts.md | 3 | done | done c4f1188 — accept_conn helper (10s accept deadline + read/write timeouts, set_nonblocking(false) for macOS accept inheritance) at every stub accept; python fakes get signal.alarm(120) self-termination; META-SPEC bounded-gates rule (--test-threads=4 + wall-clock cap); 3 new fail-fast tests; suite 6.2s |
| T7 | Fresh run rotates .chug/transcript.jsonl | specs/t7-transcript-rotation.md | 1 | done | done aed54a5 — fresh runs rotate a non-empty transcript to .chug/transcript-<ts>.jsonl before first append; --resume/chat unchanged; shares run_loop startup path with T3 (dd1322a) |
| T8 | TODO↔spec consistency guard + t1-t6 backfill | specs/t8-todo-spec-guard.md | 2 | done | done 98208e0 — tests/todo_consistency.rs guard (red before backfill, green after; deleting any specs/t*.md fails cargo test); t1/t2/t4/t5 done-records + t6 forward spec backfilled; t3 spec authored in dd1322a |
| T9 | goal_rejected message states check env | specs/t9-check-env-honesty.md | 2 | done | done ceac8f7 — rejection message names the shared run_shell wrapper + out-of-cwd prohibition; literals interpolated from tools::CARGO_BIN_REL (new const used by cargo_bin_dir) and CHECK_TIMEOUT_SECS; drift-breaks-build test + end-to-end drive_loop test |
| T10 | Append-only .chug/events.jsonl | specs/t10-events-jsonl.md | 3 | done | done 34e48de — harvested from the self session's budget-aborted run (complete + green: 318/318, clippy). Event enum stream persisted: run start, iterations w/ cumulative tokens, tool results (≤200c previews, durations), verification, goal verdicts, aborts; rotation mirrors transcript; best-effort never aborts |
| T11 | Startup banner: version/commit/cwd/model | specs/t11-startup-banner.md | 4 | done | done 881d83c — build.rs bakes CHUG_GIT_HASH (git rev-parse, "unknown" fallback, env override verified both directions); banner on stderr at run/chat start; run_start events.jsonl gains version/commit (+ chat sessions now write run_start); README updated. (Origin: 2026-09-21 META-META eval, EVALUATION.md I11 — stale-binary confusion twice) |
| T12 | Budget-abort output names model + fallback hint | specs/t12-budget-abort-hint.md | 4 | done | done af84f11 — Llm::model() getter; Event::Aborted gains model + BudgetExceeded{kind,max}; abort output prints model:/budget:/resume-with-fallback hint; eventlog abort line gains model/budget fields; README abort-output bullet; gates 327+3 green (row flip + ledger wrap landed by operator after the session's second budget abort — code was committed, bookkeeping wasn't) |
| T13 | Budget-low warning before abort | specs/t13-budget-low-warning.md | 2 | done | done 3c1c5b8 (impl 96dbe99) — WARN_REMAINING_ITERS=5 / WARN_REMAINING_SECS=300 + pure budget_low_notice helper; one-shot per-kind user-message injection in drive_loop before the LLM call (steering-note mechanism, transcript+memory); 5 new tests (boundaries, one-shot latches, interpolation, scripted max_iters=8 run). Adversarial validation PASS: 9/9 mutations caught. README bullet |
| T14 | Cumulative tokens in console abort/goal output | specs/t14-abort-token-summary.md | 4 | done | done c46136a (impl 82a38d8) — ConsoleSink caches latest cumulative Event::Usage (None until first response → line omitted); `tokens: <in> in / <out> out (cumulative)` printed after summary on GoalAccepted and with model:/budget: on Aborted; 3 with_writers tests. Adversarial validation PASS: 10/10 mutations caught. README abort-output bullet updated in impl commit |
| T15 | Token-denominated budget (--max-tokens) | specs/t15-token-budget.md | 2 | done | done 12a6d20 (impl fe822d1, harvested from a 40/40-iter glm child that died pre-commit; orchestrator fixed one test-fixture arithmetic slip) — BudgetExceeded::Tokens + top-of-iteration check on cumulative usage_in+usage_out (saturating), one-shot WARN_REMAINING_TOKENS=50_000 budget-low leg (byte-identical message when unset), run+chat CLI knobs, eventlog abort line, 7 new tests + control. Adversarial validation PASS: 8 mutants, core all died (3 weak-test survivors noted in EVALUATION Outcomes). README bullets |
| T16 | mcp_http test pins (SPEC-9 R4 carried gaps) | specs/t16-mcp-http-test-pins.md | 3 | done | done 1ec69f4 (impl 8ec604c, glm child committed + self-mutation-checked 7/7) — 4 tests in mcp_http.rs test module (+185/-0, production untouched): exact Accept-header pins (POST pair + listen GET), tools/list-over-SSE framing leg, deadline constants/constructor-default pins + behavioral first-byte-deadline test, all behind the T6 stub harness; suite 347+3 in ~9s. Tests-only item → validation per LOOP-SPEC §2.4 optional; orchestrator gates + non-vacuousness spot-check sufficed |
| T17 | Budget telemetry in events.jsonl: warning injections + configured budgets | specs/t17-budget-events.md | 3 | done | done 96cc8ef (impl 0abc5de, harvested from a 40/40-iter glm child that died pre-commit — 2nd post-T13 J1) — Event::BudgetLow emitted iff a notice is injected (fire-time remaining counts, ≤3/run), serialized as budget_low line (remaining_tokens null when unset); run_start gains max_iters/max_minutes/max_tokens at run+chat call sites; console/TUI sinks silent; warning text/latches byte-identical; README bullet. Adversarial validation VERDICT: PASS — 6/6 mutants died (2 re-killed by the signing validator; T15 adjacency smoke incl.) |
| T18 | Widen budget-low iteration warning margin (WARN_REMAINING_ITERS 5→8) | specs/t18-warn-margin.md | 2 | done | done 9056c78 — 2026-09-25 cycle-4 J6 FIRED: 3 of last 4 children died at the iteration ceiling with wrap unfinished. One constant + J6 doc line + pins + README bullet; impl glm child green first try (17 iters), spec's fire-time "7" was an off-by-one slip — remaining = max_iters − iteration at a 0-based pre-increment boundary, so max_iters=8/WARN=8 fires at remaining==8 on call 1 (adjudicated in the implementer's favor by validator + orchestrator independently). Adversarial validation VERDICT: PASS — 2/2 mutants died (revert-to-5 + <=→< flip), no pin weakened, interpolation tests correctly untouched |
| T19 | Codify the child events-harvest step in LOOP-SPEC (K2) | specs/t19-events-harvest-step.md | 3 | done | done 0dc6ec5 (impl 1e26acc, glm child green first try ~20 iters) — 2026-09-25 cycle-5 eval L1: cycle-5 orchestrator removed /tmp/chug-loop-t18 WITHOUT harvesting — both child event streams + transcripts + impl LEDGER lost (git worktree remove deletes untracked .chug/ silently). LOOP-SPEC §2 step 5 retitled "Harvest, then merge + close" with harvest-before-removal directive (events-t<N>-<role>-<ts>.jsonl impl+validator alike, both t17 precedents named, LEDGER when non-trivial, transcripts operator's choice) + Phase 3 wrap bullet; folded into step 5 (not renumbered) to preserve t13's §2.5 ref. Adversarial validation VERDICT: PASS (6/6 checks; nit fix 0dc6ec5 named both precedents). Cycle 6 practiced it: 4 T19 child artifacts harvested pre-removal |
| T20 | Startup banner + run_start gain cwd worktree HEAD (branch@commit) | specs/t20-banner-worktree-head.md | 3 | done | done f08ea04 — 2026-09-25 cycle-5 eval L2: wrong-HEAD confusion fired twice (cycle-4 T17 validator 2 killed on wrong-HEAD belief; cycle-5 T18 validator opened "I'm on main"). build_info::resolve_head (best-effort runtime git rev-parse, every failure leg → None, first runtime git call in the binary); banner gains head=<branch>@<short> only when resolvable (git-less fallback byte-identical, pinned); run_start gains head_branch/head_commit (null-when-unresolved, pinned); pure seams (no git in banner()/run_start()); 9 new tests incl. broken-.git/missing-git/detached-HEAD legs + non-vacuousness seam; README banner+events bullets. Adversarial validation VERDICT: PASS — 3/3 mutants died (render revert, resolve→None, serialization swap), zero pins weakened, never-fail proven |
| T21 | LOOP-SPEC impl-child template --max-iters 40→50 | specs/t21-child-iteration-headroom.md | 2 | done | done cac4649 — glm impl green first try (~17/40 iters, self-committed). ANOMALY: child ff-merged its own branch into main after goal_complete rejected its worktree run of the spec's check (the check greps MAIN's LOOP-SPEC.md — unsatisfiable from the worktree; spec-authoring lesson: content-checks must not cd to main); orchestrator reviewed the landed diff (+3/−1, exactly per spec) and proceeded. kimi adversarial validation VERDICT: PASS — (a) one functional change, --max-minutes 35/model/goal/polling byte-identical, META-SPEC validator template untouched; (b) cross-refs intact (step-4 fix-ups share the template, t13's §2.5 ref resolves); (c) evidence verified against all three named streams (40/40 aborts confirmed, T20 budget_low@8 + 6-of-35-minutes confirmed); (d) spec check green in main verbatim. 4 artifacts harvested pre-removal (impl+validate events/LEDGERs) |
| T22 | Bash tool description: macOS `timeout` mirage note | specs/t22-bash-timeout-note.md | 3 | done | done 6068654 (cycle 8; glm impl green first try, goal accepted 23/50 iters) — one sentence appended to the bash description with both load-bearing tokens verbatim (`no \`timeout\` command` + `perl -e 'alarm N; exec @ARGV'`), 120s driver-cap wording intact, zero behavioral surface. Pin `bash_description_warns_macos_has_no_timeout_and_pins_perl_alarm_idiom` asserts the LIVE tool_schemas() output (warning-in-context, idiom, 3→4 sentence count, final-position, cap-preservation) — not a copied literal. kimi adversarial validation VERDICT: PASS — 6/6 mutants died (revert-to-pre-T22, idiom corruption, negated-warning non-vacuousness, extra sentence, cap-wording change, note reordering); worktree restored + re-verified green post-mutations. 3 artifacts harvested (impl+validate events, validate LEDGER). Origin: 2026-09-25 cycle-7 eval M2 — TWO exit-127s in one day across two model families (T20 glm impl at ~39/40 lost 2 of its last 3 iters to the mirage, contributing to its 40/40 wrap death; cycle-7 kimi orchestrator hit it live during eval). META-SPEC:126 names the perl-alarm idiom but impl children never read META-SPEC — the tool description is the only universal surface |
| T23 | `delegate` tool: launch + status for bounded child chug runs | specs/t23-delegate-tool.md | 2 | done | done 1012dca (+nit 4169c5d, ff-merged to 4169c5d) — glm impl goal ACCEPTED at 50/50 iters (T21's widened budget + directive warning: committed at ~48, wrapped at 50 — first full wrap in the ceiling zone; mid-wrap it survived a flaky-test hunt at iters 43-47). 887-line diff: one tool two actions (launch=detached spawn, CHUG_DELEGATE_BIN seam→current_exe, <cwd>/.chug/delegate.log, returns pid/log/events at spawn; status=bounded ≤64KiB events tail via pure summarize_events + kill(pid,0) + log tail), process_group(0)+SIGHUP-IGN nohup parity, resolve_safe exemption confined+pinned, 18 new tests, README bullets. kimi validator VERDICT: PASS (10 findings, 6/6 legs: gates re-run green post-restore, 2/2 mutants died — flag-revert killed 3 pins, bound-removal killed the tail pin, non-blocking verified by reading, kill_pid_group extraction byte-identical, zero pre-T23 pins touched); nit (README comma-list) fixed by orchestrator 4169c5d. 4 artifacts harvested pre-removal |
 | T24 | LOOP-SPEC adopts the `delegate` tool for child launches + polling | specs/t24-loop-adopts-delegate.md | 2 | done | done e913cf9 (merge of impl 3334743, cycle 10) — 2026-09-25 cycle-9 eval N5: delegate (T23, 1012dca) had ZERO calls in cycle-7/8 loop streams (jq-verified); §2 taught hand-rolled nohup + ps/tail/jq polling. §2 step 2 now launches via delegate (action launch, max_iters 50/max_minutes 35 explicit — delegate defaults 40/35, T21 headroom survives), polls via delegate status (~60–110s cadence, replaces ps+tail+jq triple; liveness-unknown → ps fallback), hand-rolled nohup kept as tool-failure fallback (1 sentence, only remaining nohup mention); step 4 validator launches via delegate (kimi-k3, 40/30 per §6 budgets, §6 goal verbatim — LOOP-SPEC override of §6 mechanics; META-SPEC.md untouched); hard rule gains delegate-as-launch-surface clause; steps 1–6 unrenumbered (t13 §2.5 ref resolves). kimi adversarial validation (FIRST live delegate launch — the change dogfooded itself) VERDICT: PASS at 13/40 iters ~4 min: all 6 reqs verified, goal text byte-identical (cmp-verified), budgets match T21/T23 ground truth, gates re-run independently (build+clippy+392/3), mutation-tested (goal-corruption + 50→40 mutants caught; 2 non-blocking observations: whole-file grep non-localizing, pre-image passes check leg by design). 2 validate artifacts harvested pre-removal (impl's 2 harvested in cycle 9) | 
 | T25 | Failure-aware event previews: error tool results keep a tail window | specs/t25-failure-aware-previews.md | 2 | done | done f1f7946 (cycle 9; glm impl goal accepted 34/50 iters, self-committed) — 2026-09-25 cycle-9 eval N1: T23-impl suite FAILED once at 20:17:49 (379/380) — flat 200c head preview (driver.rs:667 take(500) → eventlog.rs:179 take(200)) lost the failing test's name; transcript unharvested; 5× suite re-runs green, no repro. Failure bytes cluster at output END — error leg gains tail-anchored ≤2000c window; ok leg byte-identical both seams. Next flake names itself from the events stream alone. VALIDATION VERDICT: PASS (kimi, 8 mutants: 7 died incl. all 3 spec-required — driver take(500)/sink take(200)/head-instead-of-tail + off-by-one/const/ok-leg mutants; 1 non-blocking survivor: driver const 2000→1999 const-relative assertions, noted). Ok legs byte-identical both seams; re-anchored 200c pin retains teeth on ok:true; README events bullet updated; 3 artifacts harvested pre-removal | 
 | T26 | `read_file` gains `offset`/`limit` pagination | specs/t26-read-file-pagination.md | 3 | done | done 179e1aa (merge of impl 66a7ab8, cycle 10) — 2026-09-25 cycle-9 eval §3: FOUR files exceeded the 2000-line read cap (driver.rs 2894, mcp_http.rs 2343, tui.rs 2163, tools.rs 2048) with no paging primitive → children sed-chunked (T20 impl burned iters 1–5). Schema gains optional integer offset (1-based, default 1) + limit (default READ_MAX_LINES, may exceed); default path byte-identical pinned (legacy note verbatim); window legs note the true window [showing lines A-B of N]; offset<1 → tool error naming 1-based; offset>EOF non-error naming length (paging loops stop cleanly); saturating arithmetic; negative/non-integer rejected; README Tools one-liner updated; spec check fixed worktree-relative pre-dispatch (1d6780d, T21 lesson). glm impl 27/50 ~5 min first-try green; kimi VERDICT: PASS — 8/8 mutants died (note drift/0-based/+limit-vs-−1/>=EOF/always-note/schema-drop/required+/full revert), 3 non-blocking observations (past-EOF contains-assertion strength, negative-offset wording, whole-file trailing-newline nuance). 3 artifacts harvested pre-removal | 
| T27 | loopd cycle budget --max-iters 80→120 | specs/t27-loopd-iteration-budget.md | 2 | done | done 759c45e (merge of impl a436baf, cycle 11) — 2026-09-25 cycle-11 eval O1: orchestrator's own iteration ceiling is now the binding budget — cycle 9 ended 79/80 (budget_low@8 21:14:00 forced directive wrap, T24 validation deferred a full cycle); cycle 7 was 76/80; freshness-skip cycles 41/68. Measured: Phase 1 ≈45 iters (eval commit at tool idx 47/91), one item end-to-end ≈30–35; minutes never binding (24–35 of 240). T21's failure class one level up. One number at loopd.sh:71 +1 comment line with the arithmetic; commit message records the no-signal-running-supervisor rationale (bash incremental-read hazard, bounded blast radius); activation = operator's next loopd restart (human-decision carry EVALUATION §6.8). glm impl 12/50 iters goal-accepted (survived a 50-min system hibernate mid-run — machine slept 21:50–22:40Z at 1% battery; macOS Instant excludes sleep so awake-time budgets held; NEW incident class, survived gracefully). kimi VERDICT: PASS 16/40 — gates re-run 403+3, goal text + mode cmp-verified, 6 mutants: 5 caught (revert-80/dup-line/121/drop-minutes/wrong-model), M2 same-line-dup escapes grep -c but clap rejects dup args at runtime (informational), M6 comment-removal in-spec-scope (verified by review). 3 artifacts harvested pre-removal |
| T28 | delegate status reaps zombie children (waitpid WNOHANG) | specs/t28-delegate-zombie-reap.md | 3 | done | done fcaa7c2 (cycle 12; impl a0a7c97 harvested uncommitted from glm child pid 44199, 50/50 budget-abort pre-commit — cycle-11 recovery executed per its own instructions; kimi VERDICT: PASS goal-accepted 34/40: gates 4x green, 4 mutants 3 caught incl. reap-disable non-vacuousness proof, M3 one-poll-lie informational survivor; live proof during validation — exited validator read STAT Z while pre-T28 status said alive:true; 4 artifacts harvested, cycle-11 partials superseded+removed) — 2026-09-25 cycle-11 eval O2: all three cycle-10 children polled alive:true AFTER state:done+goal_seen:true (21:22:47/21:31:59/21:37:39) — kill(pid,0) succeeds on unreaped zombies and the orchestrating chug never reaps. status attempts waitpid(pid, WNOHANG) first (own exited child → reaped → alive:false), ECHILD → existing kill(pid,0) semantics unchanged; launch untouched; output shape unchanged. reap_and_alive seam + pid-0/over-i32 guard; 4 tests cover spec items 1–5 |
| T29 | delegate status gains optional wait_secs long-poll | specs/t29-delegate-wait-secs.md | 3 | done | done 47dfcaf (cycle 13 — recovered mid-arc from the prior cycle's preserved worktree: impl 549d250 (glm) + validator #1 VERDICT: FAIL (M7 liveness-flip mutant SURVIVED — req 2(b) untested; deadline leg rendered stale ENTRY snapshot with no final read) → glm fix-up e36b30c (real-process liveness-flip test, sleep-30 SIGKILLed mid-wait, M7-mutant dies at 30.17s; deadline leg gains one final read + pin, revert-mutant dies 2.08s) → kimi re-validation VERDICT: PASS 36/40 (M1/M2/M3 mutants all caught incl. both fix sites, gates re-run 415+3 twice, 2 informational edge-leg nits); 8 artifacts harvested (4 events + 4 LEDGERs incl. FAIL+PASS verdict ledgers)) — 2026-09-25 cycle-11 eval O7 (FEATURE): cycle 10 spent 9/68 iterations (~13%) on instant status polls, each a full-context round trip (~325k input tokens late-cycle) — idle waiting is the loop's largest remaining mechanical token sink. status gains optional integer wait_secs (0/absent = instant, byte-identical pinned; >0 = block until events-state change / liveness flip / deadline, hard cap 600s, 2.5s cadence, +waited:<n>s line; rejected on launch naming status-only; REJECT not clamp for negative/non-integer/>600, pinned; missing-file legs wait bounded w/ creation as wake trigger, mid-wait read failure degrades instant); LOOP-SPEC §2 step-2 polling sentence gains the wait clause (ship+adopt in one item, T23→T24 pattern); README clause; 8 test legs incl. anti-fixed-sleep non-vacuousness. Schema description + delegate tool surface updated live |
| T30 | META-META-SPEC spec-bar: check: lines must be worktree-relative | specs/t30-metameta-worktree-relative-check.md | 3 | done | done 00ce617 (cycle 13; impl 4b55d61 glm 19/50 ~5 min first-try) — 2026-09-25 cycle-11 eval (cycle-10 wrap recommendation): the worktree-relative-check lesson is per-spec-author, not per-spec — T26's spec shipped a cd-to-main check (fixed pre-dispatch 1d6780d) one cycle after T24's correct one; T21's self-merge anomaly (cac4649) was the first bite. Spec-quality-bar sentence in "Extend TODO.md" extended in place (+4/−1, one hunk): the check: line MUST be worktree-relative (runs in the impl child's worktree cwd, never cd to main; content checks grep worktree, cargo test as-is), both bites cited; every other line byte-identical incl. 9b7e904's §6 README-audit. kimi adversarial validation VERDICT: PASS 19/40 — citations verified against git record (cac4649/e73b66d/1d6780d/3334743), Phase-2-sanction-vs-hard-rules coherence adjudicated (hard rules bind the evaluator role; META-META-SPEC.md not in its enumerated human-spec list), check-mutations M1/M2/M3 (drop worktree-relative / drop T21 / full revert) all killed, spec check verbatim green, gates re-run 415+3, worktree restored clean. 4 artifacts harvested |
| T31 | Deflake the parallel-load test family (mechanism, not timeouts) | specs/t31-deflake-parallel-suite.md | 1 | done | done e765de2 (impl f8012c2, glm child goal-accepted 44/50 ~11 min, budget_low@8 fired at 42 — T18 margin did its job; tests-only, all hunks in mod tests) — dead_port() probe-verified refused-port acquisition (bounded 32-attempt re-bind + pre-connect re-verify + live/dead pin dead_port_probe_distinguishes_live_from_dead) kills the bind-:0-drop TOCTOU that rejected T28's goal_complete; RUN_SHELL_TIMING_LOCK static-Mutex serializes the 3 wall-clock run_shell tests (poison-tolerant guard, std-only) so suite-manufactured scheduler stretch can no longer co-occur with a clocked window; ZERO timeout/grace/slack constants touched, all elapsed bounds byte-identical; 3 mutants killed by the child + probe-mutant independently re-killed by the orchestrator (pin dies 0.00s); 8 consecutive full-suite runs green (5 child + 3 orchestrator). Validation: tests-only per §2.4 optional (T16 precedent) — orchestrator gates + spot-check sufficed. 2 artifacts harvested pre-removal. Origin: 2026-09-25 cycle-14 eval P1 — 4 sightings / 3 named tests (dead_server_retries port TOCTOU mcp_http.rs:1484; run_shell READER_GRACE wall-clock tools.rs:1744,1782) |
| T32 | LOOP-SPEC validation-child template --max-iters 40→50 | specs/t32-validator-iteration-headroom.md | 2 | done | done a3d60ad (impl a173ff2, glm 11/50 ~3 min first-try) — one hunk, LOOP-SPEC §2 step 4 only: `max_iters: 50`, `max_minutes: 30` + parenthetical reworded "§6's budgets with T21-class widened iterations" (minutes rationale verbatim; META-SPEC.md untouched per T21/T24). kimi VERDICT: PASS 12/40 — byte-diff vs parent verified, gates 416+3 re-run independently, 4/4 budget mutants killed by the spec check, sole survivor parenthetical-only revert (by design, review-covered). SELF-REFERENCE: T32's own validator ran at the PRE-T32 40/30 budget — the 50 lands only after this merge. 2 artifacts harvested pre-removal (impl + validate streams; both LEDGERs seed-trivial, skipped per doctrine). Origin: 2026-09-25 cycle-14 eval P2 — 4 of last 6 validators ≥34/40, t29-validate1 FAIL at 39/40 |
| T33 | META-META-SPEC priority line: align with LOOP-SPEC amended doctrine | specs/t33-metameta-priority-line.md | 3 | done | done 0ee166b (impl 4b409c0, glm 16/50 ~4 min first-try) — one hunk: "bugs > robustness > features > DX friction > performance — features are first-class (LOOP-SPEC §2): at equal pri, a credible feature row is worked before a DX-friction row"; T30 check: sentence + T21/T26 bites byte-identical; LOOP-SPEC/README untouched. kimi VERDICT: PASS 12/50 — FIRST validator at the T32-widened budget; features↔robustness + DX↔performance swap mutants killed by the check; clause-drop survivor inherent to spec-dictated check (R1 met by inspection); check proven non-vacuous (fails on parent); adjudication citation verified real at EVALUATION.md:129; gates 419 re-run independently. 3 artifacts harvested pre-removal (impl + validate streams, non-trivial validate LEDGER). Origin: 2026-09-25 cycle-14 eval P3 — cycle-13 wrap handoff, adjudicated: LOOP-SPEC wins |
| T34 | LOOP-SPEC: Outcomes entries written per-item at landing, not deferred to wrap | specs/t34-per-item-outcomes.md | 3 | done | done dcbff00 (impl a965053, glm 14/50 ~4 min first-try) — two hunks, LOOP-SPEC.md only: §2 step 5 gains the per-item Outcomes directive (entry in the row-flip commit or an immediately following eval: commit; cycle-12 bite named inline with the 697a6b6 lesson quoted); Phase 3 Outcomes bullet re-scoped from wrap-authorship to wrap-assembly + gap-check. kimi VERDICT: PASS 11/50 — 3/3 check mutants killed (drop per-item / drop cycle-12 / restore old bullet), check non-vacuous on parent, grounding fact-checked (EVALUATION.md:608 "RECONSTRUCTED at cycle-13 wrap" + commit 697a6b6 real), gates 416+3 re-run. 3 artifacts harvested pre-removal (impl + validate streams, non-trivial validate LEDGER). NOTE: T31-T33 landed this cycle BEFORE the doctrine — their per-item entries are written retroactively in the cycle-14 wrap's immediately-following eval: commit, which is exactly the pattern the doctrine prescribes going forward. Origin: 2026-09-25 cycle-14 eval P4 |
| T35 | README quickstart: cargo install --path . so commands work as written | specs/t35-readme-quickstart-path.md | 3 | done | done 09730de (impl 8ebbe59, glm 10/50 ~2 min first-try) — install step immediately after `cargo build` with the PATH comment (+1/−0, README.md only, rest of Quickstart + Development byte-identical); docs-only → §2 step 4 validation optional (T16/T31 precedent): orchestrator re-ran gates independently (416+3 green worktree AND main, clippy -D warnings clean, spec check verbatim both trees) + diff review. 2 artifacts harvested pre-removal (impl events + LEDGER). Origin: 2026-09-25 cycle-14 eval §6(e) (first README-usability-audit cycle): quickstart ran `cargo build` then bare `chug` — failed as written for a cold reader (verified live: `which chug` empty, ~/.cargo/bin/chug absent, operator ran ./target/debug/chug) |
| T36 | loopd cycle budget --max-iters 120→160 | specs/t36-loopd-iteration-budget-160.md | 2 | done | done 0586e1e (impl d5141c9, glm 15/50 ~2.5 min first-try) — 2026-09-26 cycle-16 eval Q1: BOTH 120-cap fresh-eval cycles scraped the ceiling — cycle 13 at 116/120, cycle 14 at 119/120 (budget_low@8 both; T27 filed on the identical 2-data-point pattern at the 80 cap). Cost model updated for wait_secs: Phase 1 ≈45–55 (§6 audit added), item ≈28–35, wrap ≈10 → eval+3 items ≈150–165 > 120. Deferral absorbs overflow (T35 worked) but budget_low wraps squeeze Phase 3 = the next cycle's input. Minutes never binding (56–117 of 240). Fourth of the class (T21 children, T32 validators, T27 orchestrator) |
| T37 | web_fetch tool: read-only HTTP(S) GET, bounded | specs/t37-web-fetch-tool.md | 3 | done | done 1e82ffb — 2026-09-26 cycle-17: recovered per T29 precedent from the cycle-16 preserved worktree (impl 1432a2e, harvest-completed by the cycle-16 orchestrator after the glm child died 50/50 mid-gates). Gates re-run in the worktree (430+3, clippy clean) then kimi adversarial validation via delegate (REQUIRED — src/tools.rs) VERDICT: PASS 48/50 (budget_low@8 fired at 42, wrapped inside T18 margin) — all 7 reqs verified, 10/10 mutants killed (cap-20k→50k, clamp removal, redirect >=→>, scheme-gate drop, binary-accept, strip-off, truncation-removal, non-2xx-arm removal, marker corrupt, whitespace-collapse break — each died to exactly the expected test), gates green pre+post-mutations, worktree restored; 3 non-blocking observations (req-7 out-of-scope not enumerated in the impl commit msg; connect-timeout const not value-pinned; skip_raw_block prefix-matches a closing </scriptx>). Merged 1e82ffb; gates re-run in main (430+3 + clippy clean); 2 artifacts harvested pre-removal (validate events + verdict LEDGER; impl events harvested cycle-16). Origin: 2026-09-26 cycle-16 eval §4 FEATURE — LOOP-SPEC §2 names the gap (a missing delegate/web_fetch tool); delegate landed T23, web_fetch was the remaining half. Demand honest: 6 consecutive evals grep 0 organic web attempts (repo-local work) — filed on capability-gap doctrine (META-META §4, features first-class) + harness-class standard. Strictly less powerful than bash curl — value is the bounded/audited/token-safe surface (size caps, HTML strip, events previews). Shipped: GET only, http/https, redirect cap 5, connect 10s/total 30s, max_chars default 20k/clamp 100k, HTML script/style-strip + tag-strip + whitespace collapse (no new dep), text/JSON verbatim, binary refusal naming type, non-2xx tool error with ≤500c preview, [truncated at N chars] marker, sandbox candor, T6+T31 stub discipline, 9 test legs |
| T38 | Truncated-response advisory: stop_reason=max_tokens names the chunking remedy | specs/t38-max-tokens-truncation-advisory.md | 2 | done | done a686522 — 2026-09-26 cycle-18 eval R1: MAX_TOKENS=8192 (api.rs:10) truncates any single response over 8k output tokens; the T37 glm impl died 50/50 to the truncated-write saga (987-line write_file in one call) with no signal why; the driver keys off tool_use blocks and never acts on stop_reason (api.rs:417). Inject an advisory + events line at T13's seam (driver.rs:509-538) |
| T39 | delegate launch gains optional max_tokens passthrough | specs/t39-delegate-max-tokens.md | 3 | done | done 52ec8ab — 2026-09-26 cycle-19: glm impl 30/50 first-try; kimi VERDICT PASS 38/50 — 5/5 reqs hand-verified, gates re-run 444+6+3+clippy, M1/M2/M4/M5 mutants KILLED (drop-append, accept-0, drop-echo, drop-schema-minimum), M3 as_u64-for-as_i64 survivor adjudicated spec-neutral (both error legs name the constraint; 0/negatives never spawn). delegate_child_argv pure seam (absent = byte-identical pre-T39 argv, pinned whole-list + end-to-end via CHUG_DELEGATE_BIN stub), delegate_max_tokens parse (<1 → error naming constraint, 0-would-mean-unlimited rationale in doc comment), schema optional integer minimum 1, return-text echo only when configured, README launch-sentence clause. 3 artifacts harvested (impl LEDGER overwritten pre-archive by the validator's fresh-run reseed — impl events + validator verdict LEDGER kept) |
| T40 | LOOP-SPEC step-5 doctrine: TODO notes cells must contain no pipe char + run todo_consistency after edits | specs/t40-todo-pipe-doctrine.md | 3 | done | done 75c21c8 — 2026-09-26 cycle-20: recovered from cycle-19's preserved worktree (impl 311d305, glm 13/50 goal-accepted, orchestrator budget-low pre-review) per T29/T37 precedent. +7 lines LOOP-SPEC step 5, one sentence woven into the row-flip directive. kimi VERDICT: PASS 15/50 — non-vacuousness proven (3 check tokens absent on parent), gates re-run 444+6+3+clippy, M1/M2/M3 token mutants killed, M4 prose-weakening survivor by-design per T44 precedent, tree sha256-restored, citations grounded (75da192 + cycle-18 eval d264c4f). 4 artifacts harvested pre-removal (impl events copied BEFORE the validator launch — T39 reseed lesson). Origin: cycle-18 eval R2 — cycle-16's goal-gate death (pipe in T37 notes cell, goal_complete rejected ~118/120, abort 120/120, fix 75da192) |
| T41 | Sandbox-candor pass: filesystem tool descriptions name the refusal + bash escape; README Tools intro names the web_fetch exception | specs/t41-sandbox-candor-descriptions.md | 4 | done | done 7170fb4 — 2026-09-26 cycle-20: glm impl 97af780 37/50 first-try (read_file/write_file/edit_file/glob/list_dir descriptions + path properties name path-escapes-cwd refusal + bash escape hatch; README one-exception line updated to two exceptions naming web_fetch; 5 live-schema pin tests T22-style, 444->449). kimi VERDICT: PASS 36/50 — reqs 1-4 satisfied, gates re-run 449+6+6+3+clippy --all-targets, 10/10 mutants killed (incl. resolve_safe error-string corruption + surgical refusal-naming drop + clause reposition), tree restored clean, existing pins (T22/T26/delegate/path_safety) unmodified. First live T44 overlap (T42 impl concurrent, disjoint files). 3 artifacts harvested. Origin: cycle-18 eval R4 — path-escapes-cwd fired twice in cycle 16 |
| T42 | Pin webfetch connect/total timeout const values | specs/t42-webfetch-timeout-pins.md | 4 | done | done 7ff8bfa — 2026-09-26 cycle-20: glm impl 9ebd55d 28/50 first-try under the first live T44 overlap (concurrent with T41's validator, disjoint files). WEB_FETCH_CONNECT_TIMEOUT == 10s + WEB_FETCH_TOTAL_TIMEOUT == 30s pinned in webfetch.rs test module (449->452) — a const-only edit now fails the suite (T37 validator nit closed). Tests-only → validation optional per T16/T31; orchestrator gates 452+6+6+3 in main + diff review sufficed. 1 artifact harvested |
| T43 | EVALUATION.md Outcomes pruning rule (last 6 cycles full) + first compaction of cycles 5-13 | specs/t43-outcomes-pruning.md | 4 | done | done 8694003 — 2026-09-26 cycle-21: spec check bound amended 200→400 pre-dispatch (67fa2b8 — cycles 18-20 landed ~256 lines post-authorship, 200 unreachable); glm impl 4ed7bdd 20/50 first-try; LOOP-SPEC Phase 3 gains the wrap-time rule (+5 lines, else byte-identical); cycles 5-13 → 9 one-liners (30 refs verified), Outcomes 627→333; kimi VERDICT PASS 17/50 (5/5 mutants killed, ref-by-ref subject/date matching; nit 35→30 refs amended pre-merge); cycles 14-15 compacted at the same wrap (rule's first application, Outcomes 319); NEW INCIDENT CLASS: env!(CARGO_MANIFEST_DIR) baked the deleted worktree into the target-shared test binary — 2/452 false red at post-merge gates, touch+rebuild recovered, carried to next eval |
| T44 | Pipeline overlap: item N+1 impl runs while item N validates | specs/t44-pipeline-overlap.md | 1 | done | done 41f61a1 — 2026-09-26 operator speed initiative ("feels awfully slow"): serial impl→validate→merge pipeline measured — cycle 14 fresh-eval+4 items 56min. Disjoint-file impl N+1 may overlap validator N (max 2 children: 1 validator + 1 impl); merges strictly serial; doctrine items never overlap |
| T45 | Bundle trivial same-area rows into one round | specs/t45-bundle-trivial-rows.md | 2 | done | done 3efb98d — 2026-09-26 cycle-19: glm impl 28/50 first-try; kimi VERDICT PASS 20/50 — 4/4 reqs verified, gates independently re-run (436+6+3 + clippy), M3 TODO-spec-cell mutant KILLED by todo_consistency (T8 guard live), M1/M2 prose mutants by-design survivors per T44 precedent, tree hash-verified restored; 1 non-blocking finding: a bundle's delegate spec: field takes the first row's spec, child reads the rest from its worktree. §2 conjunctive eligibility (a)-(d), one commit per row in queue order, ONE validation round per bundle, shared todo: flip commit, T44 cap interplay (bundle = ONE impl child; doctrine bundle never overlaps). 4 artifacts harvested |
| T46 | Eval digest: pre-computed Phase-1 corpus summary | specs/t46-eval-digest.md | 2 | done | done 6a3fa98 — 2026-09-26 cycle-19 (freshness-skip): glm impl 49/50 goal-accepted (budget_low@8 wrapped inside T18 margin); kimi VERDICT PASS 31/50 — 5/5 reqs hand-verified vs raw jq, gates re-run twice, 6/6 kill-mutants died, 7/7 survivors (unpinned output fields) adjudicated non-blocking per T44 precedent, golden-section pin test recommended as watch item. scripts/eval-digest.sh (293 lines, jq/awk, LC_ALL=C, pinned clock, BSD/GNU fallbacks, ~0.2s) + tests/eval_digest.rs (6) + loopd refresh line + META-META-SPEC corpus item 1 digest-first + README integration; nit 6a3fa98 backticks. 4 artifacts harvested |
| T47 | Shared CARGO_TARGET_DIR for worktree builds | specs/t47-shared-target-dir.md | 3 | done | done 2f4cefc — 2026-09-26 cycle-20: glm impl c3e8942 (50/50 abort post-commit, T15-class recovery; warm-cache build 28.6s first dogfood); kimi round-1 VERDICT FAIL 21/50 — PROVEN loopd.sh export-in-while-loop persists across cycles (cycle 2+ supervisor build redirected, ./target/debug/chug stale) + M6 both-nohup-prefixes mutant survived weak >=3 pin; glm fix-up a65904f 27/50 (env-prefix on chug invocation, bare-export ban pin, per-carrier count_eq pins); kimi re-validation VERDICT PASS 42/50 — both findings verified resolved with required mutants, 8/8 mutants killed, non-vacuousness proven on parent, tree sha-restored, warm build 3.0s vs 43s-6min cold (acceptance evidence). loopd.sh mkdir + env-prefixed chug call; .gitignore target-shared/; LOOP-SPEC steps 1/2/3/4 + META-SPEC Why/§4/§5/§6 carriers; README loopd paragraph; tests/shared_target_dir.rs 6 tests. 6 artifacts harvested pre-removal |
| T48 | Test code resolves repo root at runtime, not env!(CARGO_MANIFEST_DIR) | specs/t48-test-runtime-repo-root.md | 1 | done | done d000b72 — 2026-09-26 cycle-22: glm impl 1cf0b64 (25/50, first-try green; self-recovered a git-checkout scar) — all 5 sites converted to runtime current_dir with invariant comments + new tests/no_compile_time_manifest_dir.rs pin (concatenated needle, no self-exemption, vacuity guards); zero production changes. kimi VERDICT: PASS 28/50 (11/11 mutants killed incl. pin self-mutations + all 5 bogus-path legs; tree byte-clean restored; 1 organic T31-residual dead_port_probe sighting noted). LIVE CORROBORATION mid-validation: T44-overlap x target-shared artifact-identity race fired — orchestrator t49 gate run executed the validator leftover mutant (6/6 red, self-healed); filed T52. Original notes: cycle-21 post-merge gates false-redded 2/452 via compile-time env! baking deleted worktree path; runtime current_dir (cargo runs test binaries with cwd = package root) makes binaries worktree-agnostic |
| T49 | api.rs LLM client gains a connect timeout const + value pins | specs/t49-api-connect-timeout.md | 2 | done | done 9a73b12 — 2026-09-26 cycle-23: impl 56ecf72 (cycle-22 glm 34/50) validated + merged this cycle. kimi VERDICT: PASS 17/50 — all 5 reqs verified, gates independently re-run (468 passed), 4/4 value mutants KILLED with correct const naming, builder-leg deletion survivor adjudicated spec-sanctioned evidence level (T16/T42 precedent — no test can observe the leg without a SYN-dropping endpoint), tree restored byte-clean. Original notes: api.rs reqwest builder had only .timeout(READ 600s) and reqwest has NO default connect timeout, so a blackholed endpoint stalls per attempt x T1's 8 retries (worst ~16 min); T2 arms only post-connect; siblings mcp_http/webfetch both 10s |
| T50 | loopd re-execs itself between cycles when its own script changed | specs/t50-loopd-self-reexec.md | 2 | done | done 078cb77 — 2026-09-26 cycle-23: glm impl ca11c6b (37/50 goal-accepted; FULL live smoke observed — re-exec line at next while-top, same-pid guard pass, STOP wins, scratch cleaned) + tests/loopd_reexec.rs 4 positional pins; 2 non-vacuousness mutants killed. kimi VERDICT: PASS 41/50 (target-shared-validate per T52) — gates re-run 476, 8/8 spec-shaped mutants killed, live smoke independently re-demonstrated, reqs 1-6 verified; 1 non-blocking hardening suggestion (duplicate-SELF_CKSUM additive mutant survives — exact-count pin candidate); 1 observation for next eval (host pgrep could not see the long-running chug during validation — pre-existing single-driver-check note; supervisor still launching 120 iters = premise confirmed in the wild). Original notes: cycle-22 eval S3 — running loopd predated T36/T46/T47 merges, 3 landed changes dormant 7 cycles; cksum fingerprint at start, compare + exec self at while-top (between cycles only), same-pid pidfile guard pass, STOP wins naturally, fails counter reset documented |
| T51 | README delegate paragraph: stale one-exception phrasing + spec-grade trim | specs/t51-readme-delegate-paragraph.md | 4 | done | done 372f920 — 2026-09-26 cycle-23: glm impl dd024de (26/50 first-try) — stale "one exception to cwd sandboxing" sentence replaced by the short consistent clause; spec-grade spawn mechanics (process-group/SIGHUP/nohup parity, bounded tail reads) trimmed to specs/t23-delegate-tool.md; all user-facing semantics kept (args + 40/35 defaults, max-tokens passthrough, returns-at-spawn, status surface, wait_secs semantics, bash owns worktree lifecycle); README only, net -3 lines; spec check PASS. Orchestrator gates 472 + clippy + full diff review; docs-only, kimi round skipped per T16/T31/T35. Landed under T44 overlap with T50 validator (disjoint files), merge held serial. Original notes: cycle-22 eval F1 — T41 fixed the Tools intro but the delegate paragraph still said one exception (drift moved, did not die); exception stated 3x in one section; paragraph accreted spec-grade detail |
| T52 | T44 overlap x target-shared: role-keyed target dirs (cross-checkout artifact race) | specs/t52-overlap-shared-cache-race.md | 2 | done | done 4d4c383 — 2026-09-26 cycle-23: glm impl bdba301 (28/50 first-try) — LOOP-SPEC steps 3/4/5 role-keyed dirs (validators target-shared-validate ALWAYS; overlap-window gates target-shared-gates; impls keep target-shared) + mechanism sentence, .gitignore siblings contiguous, README T47 paragraph integrated clause, loopd.sh/META-SPEC untouched, +3 pins in shared_target_dir.rs (6->9, exact-count strictly stronger than the spec >=2). kimi VERDICT: PASS 35/50 — all 6 reqs, gates re-run, 10/12 mutants killed (2 survivors spec-sanctioned doc carriers), tree byte-clean; validator dogfooded target-shared-validate live (cold 27s now warm). Original notes: cycle-22 OBSERVED mid-validation — orchestrator t49 gates executed T48 validator leftover mutant (6/6 red, self-healed); cargo metadata hash excludes checkout path, dep-info relative, mtime-freshness runs the other checkout binary; false-green leg silent |
| T53 | loopd single-driver check: ps-based, not pgrep (fails open on this host) | specs/t53-loopd-single-driver-ps-check.md | 1 | done | done 8624078 — 2026-09-26 cycle-24 (T45 bundle with T54): glm impl 5c842a6 24/50 first-try goal-accepted; kimi VERDICT PASS 24/50 (target-shared-validate per T52) — gates re-run 481, 8/8 mutants killed incl. restore-pgrep/drop-bracket/corrupt-needle/delete-pipeline/duplicate-block, live smoke re-demonstrated (real driver pid 37073 the ONLY match, negative leg exit 1). pgrep guard was dead on this host (eval I1, reproduced 9/9) failing OPEN; now ps -ax -o command= pipe grep -q bracket-needle, skip body byte-identical, loopd_reexec.rs anchor re-pinned +2 pins (pgrep gone, bracket idiom exactly-once). Original notes: cycle-24 eval I1 — pgrep persistently blind to the launchd-spawned loopd tree (9/9 invocations missed pids 37073+90114 via -f/-l/-P while ps saw both; skip fired once in supervisor history, out-of-tree driver)
| T54 | loopd_reexec.rs exact-count SELF_CKSUM pins (additive-mutant survivor) | specs/t54-loopd-cksum-exact-count-pins.md | 3 | done | done 8624078 — 2026-09-26 cycle-24 (T45 bundle with T53): glm impl 6587b22; kimi VERDICT PASS 24/50 covering the bundle — the original survivor (additive 2nd SELF_CKSUM= at while-body end) now DIES on pins 1+3, additive 2nd comparison dies on pins 2+3, delete-assignment dies on pin 1 + the pre-existing fingerprint pin; 8/8 bundle mutants killed. Three exact-count pins per T47-carrier doctrine (assignments ==1, comparisons ==1, occurrences ==2), tests-only, loopd.sh untouched, pre-existing 4 T50 pins green throughout. Original notes: cycle-24 eval I3 (T50 validator non-blocking finding, cycle-23 carry ii)
| T55 | .chug/driver.lock — same-cwd mutual exclusion for concurrent chug runs | specs/t55-driver-lock.md | 2 | done | 2026-09-26 cycle-24 eval F1 (FEATURE): two chug runs in one cwd interleave-corrupt transcript/events/LEDGER; only guards are doctrine + loopd's (dead, T53) skip. Lock acquired by run before first append; refuse iff holder alive AND argv names chug (ps, never pgrep — I1 lesson twice: PID-reuse leg); reclaim on dead/mismatch/malformed; best-effort release, SIGKILL-stale by design; chat never acquires/refuses; --resume reclaims transparently; pure holder_status seam (T28/T20 style); README bullet. done 2e7945b (impl cb5906f, cycle 25) — recovered per its own recipe: work complete but 3 compile/clippy fixes short (drive_turn→drive_loop + initial_spec arg, single_match→if let, needless borrow); orchestrator finished + committed. kimi VERDICT PASS 45/50 — 9/9 reqs, 6 mutants 4 KILLED, 2 survivors (refuse-wiring/message cluster) spec-delegated to hand-verification which the validator ran live (exit 1 + pid/remedy, zero appends, holder unaffected; stale reclaim <1s). Post-merge gates: T52-class FALSE RED (worktree-built 4-test loopd_reexec artifact reused in main, pre-T53 needle) — touch+rebuild recovered, 501 green |
| T56 | README: document loopd self-re-exec + dedupe output_truncated gloss | specs/t56-readme-loopd-reexec-and-events-dedupe.md | 4 | done | done 611af46 (impl 34554e4, cycle 25) — glm 17/50 ~2.5 min first-try; docs-only, kimi skipped per T16/T31/T35/T51 + cycle-24 handoff; orchestrator gates 501 + clippy green, spec check verbatim PASS; 2 artifacts harvested. Origin: 2026-09-26 cycle-24 eval §6 b/c: loopd section never mentions T50's re-exec (user-visible supervisor behavior, deferred at its merge); events bullet re-explains the output_truncated mechanism ~30 lines after the advisory bullet defined it (output-token ceiling appears 2x today — drift surface). README only, integrated not appended, all other sections byte-identical (carry iii strict) |
| T57 | Post-merge gates use a main-dedicated target dir (target-shared-main) | specs/t57-main-dedicated-gates-target-dir.md | 1 | done | done 2f8bbe4 (impl 1c2beee, merge 2f8bbe4, kimi PASS 18/50, cycle 26) — landed: 2026-09-26 cycle-26 eval I2: T52-class SEQUENTIAL stale artifact — post-merge gates in main reused a worktree-built pre-T53 loopd_reexec binary from target-shared via mtime freshness (3/4 false red at T55's post-merge; touch+rebuild recovered; receipt in the cycle-25 Outcomes entry). Cargo artifact names exclude the checkout path, so only a main-builders-only dir makes post-merge artifact identity correct by construction; the symmetric false-green leg would be silent. LOOP-SPEC step 5 + Phase 3 gate commands, .gitignore, README clause, shared_target_dir.rs pins. Doctrine item — runs alone, no T44 overlap; REQUIRED kimi validation |
| T58 | delegate launch gains resume:true + status summarizes the latest run segment | specs/t58-delegate-resume.md | 3 | done | done 5145536 (impl b189517 — orchestrator finish per the T55 precedent after the glm impl died 50/50 with the work complete but uncommitted, fifth occurrence of that pattern and the exact failure this feature addresses; kimi VERDICT PASS, cycle 28) — cycle-28 recovery executed per the row's own recipe: verdict read from the preserved worktree (validator goal-accepted: gates 516/516 independently re-run under target-shared-validate, 5/6 mutants killed, 1 non-blocking weak-test survivor on the max_iters/last_iteration stream-scope sentence of spec req 4); orchestrator gates independently re-run pre-merge (516 + clippy clean); merged BEFORE T61 per the row's ordering. Original row: 2026-09-26 cycle-26 eval F1 (FEATURE): delegate cannot express --resume — the cycle-18 T38 validator recovery needed a hand-rolled bash nohup resume, the exact pattern T24 replaced — and status misreports a resumed child: summarize_events scans the whole stream and latches abort_seen from the pre-resume segment (src/tools.rs:975, abort latch at :1008), so a healthy resumed validator reported state aborted (orchestrator fell back to ps). Fix: launch gains optional resume boolean (argv appends --resume, absent = byte-identical pinned, echo in return text) + verdict latches reset at each new run_start so the summary describes the latest segment. src/tools.rs — REQUIRED kimi validation |
| T59 | mcp_http dead_port tests: bounded retry on port-theft | specs/t59-mcp-http-dead-port-retry.md | 3 | done | done 062c175 (impl+merge 062c175 fast-forward, kimi skipped per T16/T31 tests-only precedent, orchestrator gates 507/507 green, cycle 27) — 2026-09-26 cycle-26 eval carry PROMOTED (T55 validator finding iii): dead_port_probe port-theft under parallel load — 3rd organic sighting (cycle-16 ~1/8, t48-validate, t55-validate; passes isolated 3/3). Between the T31 probe and the client connect a parallel test or the OS ephemeral allocator claims the port, assert_dead_port panics, gates false-red. Retry the whole acquire-probe-connect phase on detected theft (bounded 3 attempts), tests-only, mechanism not timeouts (T31 doctrine). Validation optional per T16/T31 precedent |
| T60 | Docs drift pass: driver_lock comment pin location + README src/ layout line | specs/t60-docs-drift-pass.md | 4 | done | done 2ce4f8c (impl 5f6d4d8, glm 17/50 goal-accepted ~5 min first-try, cycle 28) — docs-only, kimi skipped per T16/T31/T35/T51 precedent; orchestrator gates independently re-run 517/517 + clippy + spec check grep legs green; README brace list verified identical to the actual src/ tree (20 files + main.rs named separately). Original row: 2026-09-26 cycle-26 eval: (1) src/driver_lock.rs module comment says a static pin in tests/ keeps chat lock-free — the pin is actually the chat_turn_never_creates_or_removes_the_driver_lock unit test in src/driver.rs:3572 (T55 validator finding i, verified — no driver-lock file exists under tests/); (2) README Development layout brace-list names 18 of 20 src files — archive, driver_lock, webfetch missing, two of them this month's features (eval §6c). Docs-only, two small hunks; kimi skipped per T16/T31/T35/T51 precedent |
| T61 | resolve_safe error string names the bash fallback at fire time | specs/t61-path-escapes-error-names-bash.md | 4 | done | done c176321 (impl 9d8e613 + orchestrator nit fixes b092e00, kimi VERDICT PASS 26/50, cycle 28) — suffix at both construction sites via one shared const (prefix byte-identical, names bash, single line); pin gains bash + single-line assertions + a relative-cwd leg reaching the pop site; validator re-ran gates 517/517 under target-shared-validate and killed 5/5 mutants (suffix dropped at either site, bash corrupted, newline injected, prefix corrupted); 2 non-blocking comment nits (rustdoc re-attached to the const, leg-4 site attribution) fixed by the orchestrator pre-merge. Original row: 2026-09-26 cycle-26 eval §3: path-escapes-cwd still fires ~1x per orchestrator cycle post-T41 — T41 put the remedy in the tool descriptions but the fire-time error string is still bare path escapes cwd: {path}, and the error surface is what the model sees at the moment of need. One suffix naming bash at the resolve_safe construction site(s); prefix byte-identical (the starts_with pin keeps passing + gains a bash assertion); single line; no description text touched (T41 pins byte-sensitive). src/tools.rs — REQUIRED kimi; strictly after T58 merges (same file, no T44 overlap) |
| T62 | eval-digest golden-section pin test (T46 validator survivor class) | specs/t62-eval-digest-golden-section.md | 4 | done | done f4e3bc3 (impl 92f0243, cycle 28) — cycle-27 in-flight recovery executed per the row's recipe: the glm impl completed + self-committed + goal-accepted 2 min before cycle-27's budget wrap; diff reviewed (tests/eval_digest.rs only, +217); orchestrator gates independently re-run 508/508 + clippy clean in-worktree; kimi skipped per the row's optional-validation + T16/T31/T59 tests-only precedent; impl hand-mutation evidence (field-order swap went RED, reverted green). Original row: 2026-09-26 cycle-26 eval carry (cycle-19 watch item a): the T46 kimi validator's 7/7 survivor class — mutants in digest output fields survive because tests pin counts, never the output skeleton. Golden-section test in tests/eval_digest.rs via the existing run_digest harness: header shape, section heading order (Events files, Corpus inputs, Staleness), per-file block field order/labels, staleness field labels — volatile values by shape not literal. Tests-only; kimi optional |
| T63 | LOOP-SPEC adopts delegate resume:true as the standard child budget-death recovery | specs/t63-loop-adopts-delegate-resume-recovery.md | 2 | done | done db539c9 (impl 12c3d40, glm 29/50 first-try goal-accepted ~5 min, cycle 29) — kimi VERDICT PASS 16/50: reqs 1-3 verified, gates independently re-run 521 green under target-shared-validate, 5/5 mutants killed (leg deletion, fallback rewrap, leg moved past step 3, duplicate cap, corrupt budgets). Doctrine-only: +19/-1 step-2 leg after "Exit of the pid = child done" + new tests/loop_spec_recovery.rs 4 pins (byte-exact fallback pin, escaped non-ASCII). Original notes: 2026-09-26 cycle-29 eval I2: t58-impl died 50/50 with work complete-but-uncommitted — FIFTH occurrence of the class (t15/t17/t20 at 40/40 drove T21; t28, t47, t55, t58 at 50/50; the T58 row records "fifth occurrence … the exact failure this feature addresses"). Cost this time: cross-cycle deferral (T58+T62 wrapped in-flight cycle 27). T58 shipped resume:true + latest-segment status this cycle-triple but LOOP-SPEC step 2 never mentioned resume — the doctrine's only named recoveries were orchestrator-finish (T55) and next-cycle (T28), both costlier; cycle-18's actual recovery was hand-rolled nohup resume. Step 2 gained the recovery leg: one delegate relaunch same-worktree resume:true (1-attempt cap), then standing recipes |
| T64 | Pin the two validator survivor classes (t57 ALWAYS-form + t58 max_iters/last_iteration over-reset) | specs/t64-validator-survivor-pins.md | 4 | done | done 1859b21 (impl 95bc91a glm 48/50, cycle 30) — work complete+committed; goal_complete REJECTED by the spec check line's own defect: cargo test --lib exits 101 on this binary-only crate (pre-observed in t22/t25/t26/t29/t39/t42/t58/t59 child streams; leg 1 --test shared_target_dir passed 14/14 first). Nothing incomplete, so no T63 resume — orchestrator review + gates + independent mutant reproduction substitute per T55/T62. kimi skipped per the row's optional-validation + T16/T31/T59/T62. Pin (a) loop_spec_step5_window_carries_the_always_form_exactly_once (4 legs: MAIN 1x, ALWAYS-form 1x, byte-exact combined carrier 1x, NOT-step-3 disambiguator); pin (b) delegate_summary_run_start_gap_keeps_last_seen_max_iters_and_iteration (gap window Some(50)/Some(40) last-seen, then segment-2 values). Orchestrator gates 523/523 + clippy green under target-shared, 523/523 post-merge under target-shared-main; M5 mutant independently reproduced RED then revert-green with the old T57 pin staying green (survivor story confirmed). Original row: 2026-09-26 cycle-29 eval I3: t57-validate M5 — LOOP-SPEC step-5 ALWAYS-form rewording survives (window assertion satisfied by the mechanism sentence's "ALWAYS main checkouts"; LEDGER-t57-validate-20260926-100640); t58-validate — over-reset of max_iters/last_iteration at run_start survives (two-segment latch test src/tools.rs:2110 covers goal/abort/budget latches, not the keep-last-seen fields; LEDGER-t58-validate-20260926-064150). T54/T62 survivor-class-to-pin pipeline; tests-only, kimi optional per T16/T31/T59/T62 |
| T65 | README Continuous-mode target-cache paragraph de-accretion | specs/t65-readme-target-cache-paragraph.md | 4 | done | done 458751c (impl 99b78fa glm 19/50 first-try goal-accepted ~4 min, cycle 30) — docs-only, kimi skipped per T51/T56/T60; single hunk 16+/16- net 0, all other README sections byte-identical; two short paragraphs (user semantics: four role-keyed gitignored caches with roles + per-invocation env-prefix compressed to a clause pointing at loopd.sh + one why sentence + operator reclaims; pointer sentence naming specs t47/t52/t57); pinned tokens preserved — tests/shared_target_dir.rs untouched 14/14 green (target-shared-main/ exactly 1x README-wide, co-located with target-shared-validate/ and post-merge). Orchestrator gates 523/523 + clippy green under target-shared, 523/523 post-merge under target-shared-main; spec check legs green; 1 artifact harvested. Original row: 2026-09-26 cycle-29 eval §6(d): README.md:266-280 accreted T47+T52+T57 into one ~12-line sentence chain with three nested em-dash parentheticals; the export-in-while-loop hazard story is loopd.sh-editor-facing (lives in spec t47). Split into user semantics (four role-keyed gitignored dirs, one why sentence, operator reclaims) + pointer to specs t47/t52/t57. Pinned surface: tests/shared_target_dir.rs:505-528 pins the clause — keep green or same-diff pin update with non-vacuousness. Docs-only, kimi skipped per T51/T56/T60 |
| T66 | dead_port_probe drop→probe leg: bounded theft-retry (T59 coverage gap) | specs/t66-dead-port-probe-drop-leg-retry.md | 2 | done | done ac2ea36 (impl e418b8e glm 27/50 first-try goal-accepted ~8 min, cycle 30) — tests-only, kimi skipped per T16/T31/T59/T62; sibling driver dead_port_probe_retry_with (one attempt = bind + live-probe + drop + dead-probe; live-leg failure panics un-retried as a real regression; dead-probe-live through theft_or_regression unchanged; theft retries with a freshly bound stub, bounded DEAD_PORT_RETRY_ATTEMPTS, exhaustion names attempts + mechanism); 2 scripted-theft pins via try_clone-dup'd socket (deterministic, race-free): exactly-once + exhaustion naming; non-vacuousness both directions (gutted probe RED never masked into green, revert green). Orchestrator gates independently re-run 525/525 at --test-threads=4 AND default parallelism (the goal-gate invocation that false-red) + clippy under target-shared, 525/525 post-merge under target-shared-main at default parallelism; 1 artifact harvested. Original row: 2026-09-26 cycle-30 wrap LIVE sighting (goal-gate red): mcp_http dead_port_probe_distinguishes_live_from_dead FAILED at default-parallel cargo test — "dropped port 50956 did not refuse connections" (src/mcp_http.rs:1747). First post-T59 organic sighting (prior: cycle-16 ~1/8, t48-validate, t55-validate). T59 wrapped only the acquire-verify leg in dead_port_retry_with; the bind→drop→re-probe leg carries the identical theft window unretried. Passes isolated; 4 green full-suite runs at --test-threads=4 this cycle; nothing this cycle touched mcp_http. Fix: wrap the sequence in bounded theft-retry reusing DEAD_PORT_RETRY_ATTEMPTS + theft_or_regression discrimination (regression must panic, never retry into green) + scripted-theft deterministic pin. Tests-only, kimi optional per T16/T31/T59/T62. Gate-blocking: every cycle's goal gate is default-parallel cargo test, so this false-reds ANY future wrap |
| T67 | Spec check lines never invoke cargo test --lib (binary-only crate) + todo_consistency lint leg | specs/t67-spec-check-line-no-lib.md | 2 | done | done 0d3c88b (impl 1fa4238 glm 27/50 first-try goal-accepted ~6 min, kimi VERDICT PASS 23/50, cycle 31) — validator killed 9 mutant legs (t66 col-0 injection, t52 heading-form injection, doctrine-token corruption, condition flip, weakened-needle escape probe proving the needle load-bearing, floor corruption, t64-pin corruption, specs/t999 probe with T8-leg isolation) and independently re-ran gates 527/527 + clippy under target-shared-validate, tree cmp-restored. META-META-SPEC check-line sentence gains the never---lib rule + bites list naming the nine streams; t64 spec check line repaired --lib to --bin chug (the only live spec still carrying it — pre-fix grep matched t64 AND t67 itself, so t67's own spec self-scrubbed to the --l[i]b bracket idiom with an explanatory note: the unsatisfiable-self-gate class the row kills); todo_consistency +2 legs (lint over specs/t*.md check lines with 60-file corpus floor + t64 membership pin; doctrine pin on the no-library-targets token). Orchestrator gates 527/527 + clippy under target-shared, post-merge 527/527 + clippy under target-shared-main. 4 artifacts harvested. Original row: 2026-09-26 cycle-31 eval I2: NINE child-stream sightings (t22/t25/t26/t29/t39/t42/t58/t59/t64) of goal_complete rejected by the spec's own check line — cargo test --lib exits 101 "no library targets found" on this binary-only crate (src/main.rs, no lib target; reproduced at eval time on main). Latest: t64-impl completed AND committed, then died 50/50 on the unsatisfiable gate after leg 1 passed 14/14; T55/T62 orchestrator-finish substituted. The rejection burns the child's remaining budget and could mask real red. Fix is evaluator-side: META-META-SPEC check-line convention gains the never---lib sentence (plain cargo test or --bin chug), the T8 guard gains a lint leg over specs/t*.md check lines plus a doctrine pin, and t64's historical check line is corrected (the only live spec still carrying it). Doctrine item: REQUIRED kimi, runs alone (no T44 overlap) |
| T68 | delegate status wait_secs wakes on significant change only, not per-tool-call churn | specs/t68-delegate-wait-significant-wake.md | 3 | done | done 362e9b9 (impl 6e10195 glm 49/50 goal-accepted, budget_low at 8 wrapped inside the T18 margin, kimi VERDICT PASS 39/50, cycle 31) — DelegateSummary::significant_ne is exactly the six-field wake set (max_iters, last_iteration, budget_low_seen, goal_seen, abort_seen, abort_reason) pinned field-by-field; last_event churn renders at the deadline final read but never wakes; creation/liveness/deadline legs byte-unchanged; T29 byte-identical instant-leg pin + waited pins green unchanged; +5 pins (six-field set, churn pin — mid-wait tool_result append runs to the deadline with waited >= 3 while the payload carries the NEW last_event; significant-wake pin — mid-wait goal append returns early; live-schema wake-set pin; README clause pin). Validator: 6/6 mutants killed (any-field revert RED at 2.650s < 2.9s bound, gutted significant_ne, churn field added to wake set, goal_seen dropped, stale schema description, stale README clause), gates independently re-run 532/532 + clippy + spec check 18+13 under target-shared-validate, tree restored byte-clean; 1 non-blocking observation (churn pin 30s upper bound loose, 2.9s lower bound is load-bearing). Doc honesty at all three surfaces (doc comment, tool schema descriptions, README clause); LOOP-SPEC step-2 polling sentence deliberately untouched (orchestrator-facing, makes no wake-condition claim — validator finding 4). Orchestrator gates 532/532 + clippy under target-shared, post-merge 532/532 + clippy under target-shared-main. 3 artifacts harvested (impl ledger seed-trivial — zero update_ledger calls — skipped per T32). Original row: 2026-09-26 cycle-31 eval I3 (promoted from the cycle-29/30 watch item): delegate_status_wait returned early on ANY DelegateSummary field diff and the struct carried last_event_type/last_event_ts — an active child appends tool_result events every 2-10s, so every wait_secs 90-110 long-poll woke at 2-7s across BOTH cycles 29 and 30 (and again live this cycle during T67/T68 polling); pacing fell back to bash sleeps + instant status. T29's premise (cycle 10: 13% of orchestrator iterations on instant status polls) was unrealized while cycles wrapped at 114-115/120 with budget_low at 8. src/tools.rs: REQUIRED kimi |
| T69 | delegate gains a collect action: structured child result (verdict + goal summary + check cmd + commit refs) | specs/t69-delegate-collect.md | 2 | done | done c6ce238 (impl ccd9923 glm 50/50 budget-abort → T63 FIRST resume exercise → goal accepted 25/50 on the resumed segment, cycles 32-33) — CollectSummary/summarize_collect/render_collect in src/tools.rs: latest-segment verdict (accepted / rejected-with-later-acceptance-flip / aborted+reason / running / starting; run_start resets all four latches), FULL accepted-goal summary verbatim (multi-line safe), latest verifying cmd, bounded git refs (cap 20, optional base scopes base..HEAD, every failure leg degrades to a one-line note per the T20 resolve_head precedent); separate collect-side parse keeps DelegateSummary + the T68 six-field wake set byte-untouched (contamination + churn mutants die); dispatch legs (wait_secs status-only error, non-string base named error, optional pid liveness, unknown-action names all three actions); doc honesty at schema/README/LOOP-SPEC-step-3 surfaces. Orchestrator review-fix 138d042 (live-state-coupled verdict pin caught at review gates pre-merge). Kimi arc: round 1 (T63 resume, accepted 3/50) FAIL — impl correct on every reviewed leg, gates independently re-run 550/550 + clippy + 70/70, 8 mutants 6 caught 2 survivors (abort_reason render block dead per suite; verdict_latch leg of run_start reset vacuous); glm fix-up 50dcbe7 +68 tests-only, both mutants RED-proven; round 2 (accepted 45/50) FAIL — both survivors confirmed killed, tests-only confirmed, T68 contamination/churn mutants die, ONE new same-class survivor (check_cmd latch reset); glm fix-up2 88227ab +4, mutant RED-proven; round 3 (accepted 22/50) PASS 0 findings — all four run_start latch resets swept each with a killing test, gates 551/551 + clippy + 71/71 under target-shared-validate, tree byte-clean. Orchestrator gates 551/551 + clippy under target-shared, post-merge 551/551 + clippy under target-shared-main. FEATURES.md F1 checked off. 6 child-run artifacts + 2 ledgers harvested. Original row: 2026-09-26 cycle-32 ROADMAP PULL — FEATURES.md F1 Tier-1 top unworked (operator directive 259b5ed + dc18a7a: next fresh eval MUST pull F1 then F13; F13 is the NEXT eval's mandated pull, one item per pull per FEATURES.md working rules). Today a finished child's result is git/files archaeology: git log for commit refs, jq events.jsonl for the goal summary, delegate.log scroll for gates — three hand-rolled surfaces re-derived per use (benchmark: Claude Code Task returns one structured result). Sources already exist: events goal line carries outcome+summary (eventlog.rs:212), verifying carries cmd (:207), commit refs = bounded git log with T20 resolve_head degrade precedent. Collect is bounded + non-blocking, segment-scoped per T58, six-field T68 wake set untouched (contamination = named validator mutation); carries its own LOOP-SPEC §2 step 3 adoption sentence (T23→T24 zero-calls lesson N5) |
| T70 | decision_log tool: structured loop decision records (.chug/decisions.jsonl) + LOOP-SPEC adoption — F13 phase 1 | specs/t70-decision-log-tool.md | 2 | done | done 572ec5a (impl b98ca1c glm 50/50 abort → T63 THIRD resume goal accepted 18/50; first goal-gate rejection was a cold-build flake — api.rs body_watchdog timing suspected, 4x green re-runs, new watch item; fix-up f50fac6 tests-only +37/-1: hardcoded literal PINNED_SEED_CLASSES + exact rendered-list leg, class swept, M1a RED-proven; kimi round 1 FAIL 1 finding vacuous schema-pin leg; kimi round 2 PASS 0 blocking findings goal accepted 42/50 — fix-verification mutants ALL RED + 13 impl mutants ALL RED, 3 survivors non-blocking observation-class; post-merge gates 559/559 + clippy green in main under target-shared-main; validator events + ledger harvested) |
| T71 | Extract the delegate surface from src/tools.rs into src/delegate.rs (pure move) | specs/t71-delegate-extraction.md | 3 | done | done e1e940c (impl 3b732c9 glm 50/50 budget-abort with work complete-uncommitted → T63 FOURTH resume goal accepted 19/50; tools.rs 5237→1783, delegate.rs +3485, 79 tests moved, partition 133 = 54 retained + 79 moved all byte-identical; schema json! + dispatch arm byte-identical, arm → crate::delegate::delegate; no shim/allow/renames; delegate count 71/448 exact match; hand-check RED-proven; 2nd sighting api.rs body_watchdog cold-parallel flake, re-ran green 3×; kimi VERDICT PASS 0 blocking findings 50/50 — 4 structural mutants ALL caught, BEFORE count independently re-run 71/448, visibility-widen class closed by exhaustive diff; post-merge gates 559/559 + clippy green in main under target-shared-main; 4 artifacts harvested) |
| T72 | LOOP-SPEC §2 step 4 FAIL arc gains the sweep-the-family directive | specs/t72-sweep-the-family.md | 3 | done | done db74a99 (impl 8a8e18a glm first-try goal accepted 26/50: ONE in-place hunk at the FAIL-arc sentence, no renumbering + tests/loop_spec_sweep_family.rs 192 lines T63/T64 precedent, deletion hand-check RED-proven 3/3; kimi VERDICT PASS 0 blocking findings 20/50 — 7 doc-side mutants all behaved as required incl. weakened-pin escape-then-RED proving load-bearing, class swept no survivors, 3 non-blocking observations; orchestrator gates 563/563 + clippy pre-merge, post-merge gates in main under target-shared-main; 5 artifacts harvested) |
| T73 | chug plan: read-only planning mode (F2 phase 1) | specs/t73-plan-mode.md | 2 | done | done 87fe53f (impl dbda51a glm 50/50 abort → T63 FIFTH resume accepted 48/50 cycle 36; fix-up 3b90103 glm 50/50 abort → T63 SEVENTH resume accepted 12/50 cycle 37; kimi round 1 VERDICT FAIL weak-tests-not-correctness — 3 survivors one class: run_plan_loop startup/exit guarantees unpinned at loop level, concrete Client blocked ScriptedLlm → round 2 VERDICT PASS 0 blocking findings 46/50, 9 mutants all RED) — fix-up applied the run_turn seam pattern (run_plan_loop takes &mut dyn Llm + McpRegistry, ZERO impl semantic changes) + 5 loop-level killing tests each RED-proven (M7 run_start mode, M7b ensure_seeded, M7c archive_stale, M11 dropped is_error guard = the real exit-semantics hole, M-ext live-MCP-registry — old empty-registry pin proven vacuous for the class, kept for its own leg; hand-written run_start mirror deleted); orchestrator gates 585/585 + clippy under target-shared, post-merge 585/585 + clippy under target-shared-main; merge resolved 7 add/add driver.rs hunks --theirs byte-exact (errant cherry-pick 5464878 in ancestry, patch-identical to dbda51a, never force-push); validator non-blocking: TMPDIR-shared sandbox-escape pin hygiene + schema minLength nit + MCP leg premise-asserted; 5 artifacts harvested cycle 37 (9 total); phase 2 deferred (/plan chat + --approve gate + web_fetch-in-plan), reason EVALUATION.md cycle-36 §4 |
| T74 | api.rs body_watchdog cold-parallel timing flake (2 sightings one day) | specs/t74-body-watchdog-flake.md | 2 | done | done ef0c3eb (impl 0f1475e glm first-try goal accepted 46/50; kimi VERDICT PASS 0 blocking findings 24/50) — aborts_silent upper 1900ms to 15s keeping the activity-vs-600s-total discrimination (40x under, 900ms not-instant lower kept), allows_slow_steady 250ms x5 to 50ms x60 (total 3x timeout, gaps 20x under, load-monotone), family sweep documented in-code (classification test class-only, deadline never arms, no other margin leg); RED-proofs both directions in the commit; acceptance 10/10 consecutive default-parallel full suites 563/563; orchestrator gates independently re-run 563/563 + clippy under target-shared, post-merge 563/563 + clippy under target-shared-main; validator re-ran gates under target-shared-validate + 3 more default-parallel suites, 3 mutants all RED (gutted, total-deadline, instant-fire 811us proving the lower bound load-bearing), 2 non-blocking observations; impl child logged 5 organic decision_log records + validator 1 (adoption datapoint); 5 artifacts harvested |
| T75 | LOOP-SPEC wrap checklist gains the decision-records sentence (zero-call adoption gap) | specs/t75-decision-log-adoption.md | 3 | todo | 2026-09-26 cycle-36 eval I3: T70 landed decision_log + LOOP-SPEC adoption at 4 named points (merge 572ec5a) but cycles 34+35 made 9 routing/verdict decisions with ZERO decision_log calls (digest tool distributions both orchestrator streams) and .chug/decisions.jsonl does not exist — the T23→T24 zero-calls lesson (named-point sentences alone did not wire the surface into the arc; nothing asks for the records at accounting time). F13 phase 2's corpus precondition depends on these records existing. Fix: ONE in-place insertion in LOOP-SPEC Phase-3 wrap checklist (decisions.jsonl carries the cycle's records — eval-triage / recovery-routing / model-fallback / validation-routing / validation-verdict / outcome backfills — and a worked-item cycle with zero records is an incomplete wrap, citing T23→T24 + cycles 34-35) + tests/loop_spec_decision_records.rs pin (T63/T64/T72 precedent: needles exactly-once inside the Phase-3 window, deletion RED-proven). Doctrine row: REQUIRED kimi, runs alone (no T44 overlap) |
| T76 | tgrep: token-budgeted ranked context retrieval | specs/t76-tgrep-context-tool.md | 1 | todo | 2026-09-26 operator ("tgrep or something for relevant context"), Langfuse telemetry: 9.5M in/1.56M out 2 days, kimi 79%; context bulk = full-file reads (driver.rs ~40k) + wide bash output. Ranked match clusters under a token budget + symbols skeleton mode; no new deps |
| T77 | Cache-stable transcript trimming (segment-frozen prefix) | specs/t77-cache-stable-trim.md | 2 | todo | 2026-09-26 telemetry: avg ~6k uncached/call vs ~100k transcript (proxy zeroes cache_read, unmeasurable but real). Trim's mid-history byte edits can bust the cache prefix every event. Fixed 16k segments, frozen once collapsed, one marker per segment |
| T78 | Release builds for the loop (binary + gates) | specs/t78-release-builds.md | 1 | todo | 2026-09-26 operator knob request ("compiled binary"): everything runs DEBUG — loopd, children, validators, gates; ~85% of item time is tool execution and debug codegen is 2-5x slower there. cargo build/test --release everywhere in loop paths |
| T80 | Docs-only rounds skip the cargo gates | specs/t80-docs-only-gates.md | 2 | todo | 2026-09-26 operator speed initiative: *.md-only diffs pay 500+ tests 2-4x per item (review/validation/post-merge) when the suite cannot go red. Gates shrink to todo_consistency + bash -n; validator keeps escape clause for executable doc text (T67 class) |
