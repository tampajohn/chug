# TODO

| id | title | spec | pri | status | notes |
|----|-------|------|-----|--------|-------|
| T1 | API retry survives endpoint restarts | specs/t1-api-retry-endpoint-restart.md | 1 | done | done 9840aba — 2026-09-20: two meta sessions died to transient muse resets (conn reset ~30s) — chug retries only ~15s (4 attempts, 1-8s backoff). Connection-level errors (reset/refused/timeout) should retry for minutes (e.g. 8 attempts over ~5 min); 4xx stays fail-fast |
| T2 | Per-request activity timeout on LLM calls | specs/t2-llm-activity-timeout.md | 2 | done | done 9840aba — 2026-09-20: a round-2 child hung 5+ min at 0% CPU on a stale HTTP connection; only the 600s read timeout saved it (and the wedge-kill got there first). Add ~180s no-bytes timeout distinct from total read timeout |
| T3 | Fresh run detects spec/ledger mismatch | specs/t3-stale-ledger-detection.md | 2 | todo | 2026-09-21: a fresh `chug run --spec SPEC-9` in the repo inherited the OLD SPEC-5 meta's LEDGER.md ("goal met") because ensure_seeded skips when a ledger exists. New runs should compare the goal/spec against the ledger's project and archive+reseed on mismatch (or always seed a run-scoped ledger) |
| T4 | Bash tool prepends ~/.cargo/bin to PATH | specs/t4-bash-cargo-path.md | 1 | done | done 9840aba — 2026-09-21 transcript mining: nearly every command in every session starts with `export PATH="$HOME/.cargo/bin:$PATH"` — children and metas each had to discover `cargo: command not found` individually (several first attempts died outright). chug's bash tool should prepend ~/.cargo/bin itself — it's our own toolchain |
| T5 | edit_file non-unique match returns match line numbers | specs/t5-edit-disambiguate.md | 2 | done | done 9840aba — 2026-09-21 transcript mining: `edit_file: 'old' found 2 times` triggered full-file `git checkout --` reverts + rewrites (3x in one round) instead of a second anchored attempt. On non-unique match, return the line numbers (+few lines context) of every match so the model can disambiguate |
| T6 | HTTP stub tests must not hang gates | specs/t6-stub-test-timeouts.md | 3 | todo | 2026-09-21: round-2's HTTP stub tests made a gates bash run 9 minutes (and a cargo test 4 minutes), freezing the meta long enough to look wedged. Stub server needs internal timeouts; review/validation commands should use --test-threads and bounded waits |
| T7 | Fresh run rotates .chug/transcript.jsonl | specs/t7-transcript-rotation.md | 1 | todo | 2026-09-21 META-META eval (EVALUATION.md I8): transcript.jsonl holds 7 spliced sessions (SPEC-4 goal at line 1 … META-META at 509); fresh runs append (driver.rs:196-207) and --resume loads the whole pile (driver.rs:249-255) — a resume today splices foreign sessions into context. Pair with T3 (same startup path) |
| T8 | TODO↔spec consistency guard + t1-t6 backfill | specs/t8-todo-spec-guard.md | 2 | todo | 2026-09-21 META-META eval (EVALUATION.md I10): specs/ never existed in git (git log --all -- specs/ empty) though TODO T1-T6 reference it; 9840aba touched only api.rs+tools.rs — SELF closed rows without specs and nothing noticed. Add a parsing test + backfill t1-t6 done-records |
| T9 | goal_rejected message states check env | specs/t9-check-env-honesty.md | 2 | todo | 2026-09-21 META-META eval (EVALUATION.md I7): check failed on cargo PATH → agent created a symlink in ~/.claude/plugins/cache (outside sandbox, still on disk) to pass verification (transcript 479-489). Rejection text (driver.rs verify) gives no env context; state that the check runs via the same run_shell as bash (T4 PATH prepend) + warn against out-of-cwd changes |
| T10 | Append-only .chug/events.jsonl | specs/t10-events-jsonl.md | 3 | todo | 2026-09-21 META-META eval (EVALUATION.md §3/§4): postmortems mine transcript.jsonl — 231 of 510 lines are [trimmed] and it splices sessions; only durable local log is risk_verdicts.jsonl (riskgate.rs:132). Persist the driver's event stream locally (iteration/tool/goal/abort, best-effort, never aborts the run) |
| T11 | Startup banner: version/commit/cwd/model | specs/t11-startup-banner.md | 4 | todo | 2026-09-21 META-META eval (EVALUATION.md I11): stale-binary confusion twice (LEDGER-spec5-archive smoke gotcha; SPEC-9 orchestrator ran a pre-T4 binary so the check-env fix didn't apply — transcript 501). Print one line at run/chat start so staleness is visible |
| T12 | Budget-abort output names model + fallback hint | specs/t12-budget-abort-hint.md | 4 | todo | 2026-09-21 META-META eval (EVALUATION.md I9): muse-glimmer-30b died 3× on 40-iter budgets in SPEC-9 round 2 (transcript 386/394; LEDGER-spec9-archive) before manual kimi fallback; validation budget needed 25→40 (7108ec4). Abort text doesn't name the model or suggest `chug run --resume --model <other>` |
